---
quick_task: 260323-i97
title: "Dashboard attribution from config"
requirement: DATA-07
completed: "2026-03-23"
duration: "~5 min"
commit: f7b74c5
tags: [dashboard, attribution, config-driven, data-07]
key-files:
  modified:
    - src/dashboard/tabs/overview.py
    - src/dashboard/tabs/segments.py
    - src/dashboard/tabs/diagnostics.py
    - src/dashboard/tabs/drivers.py
    - tests/test_dashboard.py
---

# Quick Task 260323-i97: Dashboard Attribution from Config Summary

**One-liner:** Replaced 6 hardcoded "Sources: World Bank Open Data, OECD.Stat, LSEG Workspace" strings across all 4 dashboard tab files with a module-level `_ATTRIBUTION_TEXT` constant built from `SOURCE_ATTRIBUTION` in `ai.yaml`.

## What Changed

### Pattern applied to all 4 tab files

Each tab file now imports `SOURCE_ATTRIBUTION` from `src.dashboard.app` and builds the attribution string at module load time:

```python
from src.dashboard.app import ..., SOURCE_ATTRIBUTION

_ATTRIBUTION_TEXT = "Sources: " + ", ".join(SOURCE_ATTRIBUTION.values())
```

The config values (`world_bank`, `oecd`, `lseg` keys in `ai.yaml`) produce:
`"Sources: World Bank Open Data (data.worldbank.org), OECD.Stat (stats.oecd.org), LSEG Workspace (Refinitiv)"`

### Per-file changes

| File | Occurrences replaced | Notes |
|------|---------------------|-------|
| `overview.py` | 2 | Fan chart card, bar chart card |
| `segments.py` | 2 | All-segments grid (per card), single-segment view |
| `diagnostics.py` | 2 | Scorecard card (+ residuals suffix), backtest card (+ CV suffix) |
| `drivers.py` | 1 | SHAP chart card |

**diagnostics.py** had attributions with extra context suffixes — preserved by string concatenation:
- `_ATTRIBUTION_TEXT + " · Residuals from statistical baseline model"`
- `_ATTRIBUTION_TEXT + " · Backtesting via expanding-window temporal cross-validation"`

### Test added: `test_tab_attribution_from_config`

Verifies two invariants:
1. Each tab module has `_ATTRIBUTION_TEXT` that starts with the config-derived prefix (fails if import wiring is broken)
2. The hardcoded literal `"World Bank Open Data, OECD.Stat, LSEG Workspace"` is absent from each module's source (regression guard)

## Test Results

```
8 passed in 0.27s  (full tests/test_dashboard.py suite)
```

## Deviations from Plan

None — executed exactly as described.

## Self-Check: PASSED

- Commit `f7b74c5` exists: confirmed
- No hardcoded attribution strings remain in `src/dashboard/tabs/`: confirmed (grep clean)
- All 8 dashboard tests pass: confirmed
