---
phase: 11-dashboard-and-diagnostics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/app.py
  - src/dashboard/tabs/overview.py
  - src/dashboard/charts/fan_chart.py
  - src/dashboard/tabs/segments.py
  - src/dashboard/tabs/diagnostics.py
  - src/dashboard/tabs/__init__.py
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "No usd_point or usd_ci* alias columns exist in FORECASTS_DF at runtime"
    - "No PCA, composite index, or multiplier derivation text appears in any dashboard UI"
    - "All tabs render with real USD column names (point_estimate_real_2020, ci80_lower, etc.)"
    - "BACKTESTING_DF is loaded at app startup from backtesting_results.parquet"
    - "All 8 existing dashboard tests still pass after alias removal"
  artifacts:
    - path: "src/dashboard/app.py"
      provides: "Clean startup without alias columns, plus BACKTESTING_DF load"
    - path: "src/dashboard/tabs/overview.py"
      provides: "Overview tab using direct column names, PCA references removed"
    - path: "src/dashboard/charts/fan_chart.py"
      provides: "Fan chart using direct column names, PCA y-label removed"
  key_links:
    - from: "src/dashboard/tabs/overview.py"
      to: "FORECASTS_DF"
      via: "point_estimate_real_2020 instead of usd_point"
      pattern: "point_estimate_real_2020"
    - from: "src/dashboard/charts/fan_chart.py"
      to: "FORECASTS_DF"
      via: "ci80_lower instead of usd_ci80_lower"
      pattern: "ci80_lower"
    - from: "src/dashboard/app.py"
      to: "backtesting_results.parquet"
      via: "BACKTESTING_DF global"
      pattern: "BACKTESTING_DF"
---

<objective>
Remove the 5 pass-through alias columns from app.py, update all callsites to use native column names, remove all PCA/composite index/multiplier derivation text from dashboard UI, and add BACKTESTING_DF load at startup. This is the dangerous coordinated cross-file refactor that must be done atomically.

Purpose: DASH-04 requires Normal/Expert modes to show real USD figures with all composite index references removed. The alias removal is the prerequisite for all downstream dashboard work.
Output: Clean app.py without aliases, all tabs using native column names, zero PCA/composite references in UI text.
</objective>

<execution_context>
@/Users/simonleowegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonleowegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-dashboard-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-dashboard-and-diagnostics/11-UI-SPEC.md

<interfaces>
<!-- Current alias block to DELETE from app.py (lines 44-52): -->
```python
# DELETE these 5 lines:
FORECASTS_DF["usd_point"] = FORECASTS_DF["point_estimate_real_2020"]
FORECASTS_DF["usd_ci80_lower"] = FORECASTS_DF["ci80_lower"]
FORECASTS_DF["usd_ci80_upper"] = FORECASTS_DF["ci80_upper"]
FORECASTS_DF["usd_ci95_lower"] = FORECASTS_DF["ci95_lower"]
FORECASTS_DF["usd_ci95_upper"] = FORECASTS_DF["ci95_upper"]
```

<!-- Callsite mapping from 11-RESEARCH.md Pattern 3: -->
| File | Old column | New column |
|------|-----------|------------|
| overview.py lines 100-105, 111-116 | usd_point, usd_ci80_lower, usd_ci80_upper, usd_ci95_lower, usd_ci95_upper | point_estimate_real_2020, ci80_lower, ci80_upper, ci95_lower, ci95_upper |
| overview.py line 323 | usd_point | point_estimate_real_2020 |
| overview.py line 452 | usd_point | point_estimate_real_2020 |
| fan_chart.py lines 67-71 | usd_point, usd_ci80_*, usd_ci95_* | point_estimate_real_2020, ci80_lower, ci80_upper, ci95_lower, ci95_upper |

<!-- PCA/composite strings to remove (from RESEARCH.md State of the Art): -->
- overview.py line 174: "Raw composite index at 2030 = ..."
- overview.py lines 197-199: "Expert mode: Y-axis shows the raw composite index (PCA first principal..."
- overview.py line 202: fan_title = "Forecast Fan Chart — Raw Composite Index"
- overview.py lines 244-245: "Raw composite index values at 2030 per segment..."
- overview.py lines 257-258: "PCA scores centered at 0..."
- overview.py line 288: "raw index + multiplier derivation panel"
- overview.py lines 458-459: y_title_2030/y_title_ts "Composite Index" labels
- fan_chart.py line 216: y_label = "Composite Index (PCA score)"
- fan_chart.py line 13 (docstring): "raw composite index is shown"
- segments.py line 116: "Expert mode: Y-axis shows raw composite index values (PCA scores)."
- segments.py line 148: "StandardScaler for PCA"
- diagnostics.py line 362: "StandardScaler for PCA composites"
- tabs/__init__.py line 12: "composite index values, multiplier derivation"

<!-- Replacement text (UI-SPEC Copywriting): -->
"Anchor-calibrated USD model (v1.1) — trained on real USD market size series 2017–2024"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove alias columns from app.py and add BACKTESTING_DF load</name>
  <files>
    src/dashboard/app.py
  </files>
  <read_first>
    .planning/phases/11-dashboard-and-diagnostics/11-RESEARCH.md,
    src/dashboard/app.py
  </read_first>
  <action>
1. **Delete alias block** — Remove lines 44-52 of app.py (the comment block "Minimal pass-through..." and the 5 FORECASTS_DF alias assignments). Also update the module docstring to remove the sentence about "Pass-through alias columns (usd_point etc.) are preserved for tab compatibility until Phase 11 refactors all tabs...".

2. **Add BACKTESTING_DF load** — After the RESIDUALS_DF line, add:
   ```python
   BACKTESTING_DF = pd.read_parquet(DATA_PROCESSED / "backtesting_results.parquet")
   ```

3. **Update DIAGNOSTICS computation** — The existing DIAGNOSTICS dict computes RMSE from residuals and sets mape/r2 to "N/A". Now that backtesting_results.parquet is loaded, update to compute from BACKTESTING_DF:
   ```python
   # Compute diagnostics from backtesting results (replaces residuals-only RMSE)
   DIAGNOSTICS = {}
   for segment, grp in BACKTESTING_DF.groupby("segment"):
       hard_rows = grp[grp["actual_type"] == "hard"]
       DIAGNOSTICS[segment] = {
           "mape": float(hard_rows["mape"].mean()) if not hard_rows.empty else None,
           "r2": float(hard_rows["r2"].mean()) if not hard_rows.empty else None,
           "mape_label": hard_rows["mape_label"].iloc[0] if not hard_rows.empty else "no_hard_actuals",
           "has_hard_actuals": not hard_rows.empty,
       }
   ```

4. **Keep RESIDUALS_DF load** — Don't remove it yet; the existing backtest chart in `backtest.py` and `diagnostics.py` still import RESIDUALS_DF. Plan 11-04 will rewrite those to use BACKTESTING_DF.

DO NOT touch any other file in this task — alias callsite updates happen in Task 2.
  </action>
  <verify>
    <automated>cd /Users/simonleowegner/my-project && uv run python -c "from src.dashboard.app import FORECASTS_DF, BACKTESTING_DF; assert 'usd_point' not in FORECASTS_DF.columns; assert 'BACKTESTING_DF' is not None; print('OK: aliases removed, BACKTESTING_DF loaded')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v "^#" src/dashboard/app.py | grep -c "usd_point" returns 0 (no non-comment usd_point references)
    - grep -q "BACKTESTING_DF" src/dashboard/app.py
    - grep -q "backtesting_results.parquet" src/dashboard/app.py
    - The string "usd_ci80_lower" does NOT appear as an assignment in app.py
  </acceptance_criteria>
  <done>app.py no longer creates alias columns. BACKTESTING_DF is loaded at startup. DIAGNOSTICS dict populated from backtesting results.</done>
</task>

<task type="auto">
  <name>Task 2: Update all callsites and remove PCA/composite references from UI</name>
  <files>
    src/dashboard/tabs/overview.py,
    src/dashboard/charts/fan_chart.py,
    src/dashboard/tabs/segments.py,
    src/dashboard/tabs/diagnostics.py,
    src/dashboard/tabs/__init__.py
  </files>
  <read_first>
    .planning/phases/11-dashboard-and-diagnostics/11-UI-SPEC.md,
    .planning/phases/11-dashboard-and-diagnostics/11-RESEARCH.md,
    src/dashboard/tabs/overview.py,
    src/dashboard/charts/fan_chart.py,
    src/dashboard/tabs/segments.py,
    src/dashboard/tabs/diagnostics.py,
    src/dashboard/tabs/__init__.py
  </read_first>
  <action>
ALL changes in this task must be done before running any tests — a partial edit will break imports.

**overview.py alias replacement:**
- Replace all `"usd_point"` with `"point_estimate_real_2020"` (lines ~100, 111, 323, 452)
- Replace all `"usd_ci80_lower"` with `"ci80_lower"` (lines ~102, 113)
- Replace all `"usd_ci80_upper"` with `"ci80_upper"` (lines ~103, 114)
- Replace all `"usd_ci95_lower"` with `"ci95_lower"` (lines ~104, 115)
- Replace all `"usd_ci95_upper"` with `"ci95_upper"` (lines ~105, 116)

**overview.py PCA/composite text removal:**
- Line ~174: Replace `f"Raw composite index at 2030 = {idx_2030:.4f} · "` with appropriate real USD text: `f"2030 market size forecast · "`
- Lines ~197-199: Replace the expert mode fan chart description ("Y-axis shows the raw composite index (PCA first principal component...negative values are valid PCA scores...") with: `"Expert mode: Y-axis shows real 2020 USD market size. Anchor-calibrated USD model (v1.1) — trained on real USD market size series 2017–2024."` (UI-SPEC Copywriting)
- Line ~202: Replace `fan_title = "Forecast Fan Chart — Raw Composite Index"` with `fan_title = "Forecast Fan Chart — USD Market Size"`
- Lines ~244-245: Replace "Raw composite index values at 2030 per segment (before USD conversion). These are PCA scores..." with `"2030 USD market size forecast per segment. Values in real 2020 USD billions."`
- Lines ~257-258: Replace "Year-by-year raw composite index..." with `f"Year-by-year USD market size forecast for the {display_name} segment. Values in real 2020 USD billions."`
- Line ~288: Replace "raw index + multiplier derivation panel" comment with `"Expert mode: model methodology panel"`
- Lines ~458-459: Replace `y_title_2030 = "Composite Index (2030)"` with `y_title_2030 = "USD Billions (2030)"` and `y_title_ts = "Composite Index (PCA score)"` with `y_title_ts = "USD Billions (Real 2020)"`
- Remove or rewrite the entire `_build_expert_methodology_card` function if it references PCA/multiplier derivation — replace content with v1.1 model description: "Anchor-calibrated USD model (v1.1) — trained on real USD market size series 2017–2024" per UI-SPEC Copywriting.

**fan_chart.py updates:**
- Line 13 (docstring): Replace "raw composite index is shown" with "real 2020 USD is shown"
- Lines 67-71: Replace `usd_point` → `point_estimate_real_2020`, `usd_ci80_lower` → `ci80_lower`, etc. across the usd_mode=True branch
- Line 216: Replace `y_label = "Composite Index (PCA score)"` with `y_label = "USD Billions (Real 2020)"`
- Remove the `usd_mode` parameter entirely if feasible (since all modes now use USD) OR keep it but update both branches to use correct column names.

**segments.py PCA text removal:**
- Line 116: Replace "Expert mode: Y-axis shows raw composite index values (PCA scores)." with `"Expert mode: Y-axis shows real 2020 USD market size values."`
- Line 148: Replace "StandardScaler for PCA" with `"expanding-window cross-validation"`

**diagnostics.py PCA text removal:**
- Line 362: Replace "StandardScaler for PCA composites is fit on training fold only" with `"Preprocessing fit on training fold only — no data leakage"`

**tabs/__init__.py:**
- Line 12: Replace "composite index values, multiplier derivation" with `"real USD market size values, model methodology"`

Run `uv run pytest tests/test_dashboard.py -q` after ALL edits are complete. Per Pitfall 1 in RESEARCH.md, partial edits will cause KeyError: 'usd_point'.
  </action>
  <verify>
    <automated>cd /Users/simonleowegner/my-project && uv run pytest tests/test_dashboard.py -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "usd_point" src/dashboard/tabs/ src/dashboard/charts/fan_chart.py returns 0 matches (excluding comments)
    - grep -rn "usd_ci80" src/dashboard/tabs/ src/dashboard/charts/fan_chart.py returns 0 matches
    - grep -rn '"PCA' src/dashboard/tabs/ returns 0 matches (no PCA in display strings)
    - grep -rn "Composite Index" src/dashboard/tabs/ src/dashboard/charts/ returns 0 matches
    - grep -rn "multiplier derivation" src/dashboard/tabs/ returns 0 matches
    - grep -q "point_estimate_real_2020" src/dashboard/tabs/overview.py (confirms replacement)
    - grep -q "USD Billions" src/dashboard/charts/fan_chart.py (confirms new y-label)
    - All 8 existing dashboard tests pass
  </acceptance_criteria>
  <done>All alias references replaced with native column names. All PCA/composite index/multiplier text removed from dashboard UI. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_dashboard.py -q --timeout=30` — all 8 existing tests pass
2. `grep -rn "usd_point\|usd_ci80\|usd_ci95" src/dashboard/ | grep -v "^.*:#"` — zero non-comment matches
3. `grep -rn "PCA\|composite index\|multiplier derivation" src/dashboard/tabs/ src/dashboard/charts/ | grep -v "^.*:#"` — zero non-comment matches
4. `uv run python -c "from src.dashboard.app import BACKTESTING_DF; print(f'Loaded {len(BACKTESTING_DF)} rows')"` — prints "Loaded 12 rows"
</verification>

<success_criteria>
- Zero alias column references remain in dashboard source code
- Zero PCA/composite index/multiplier references remain in dashboard UI text
- BACKTESTING_DF loaded at startup with 12 rows
- All 8 existing dashboard tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-and-diagnostics/11-02-SUMMARY.md`
</output>
