---
phase: 03-ml-ensemble-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/models/ml/__init__.py
  - src/models/ml/gradient_boost.py
  - src/models/ml/quantile_models.py
  - tests/test_ml_models.py
autonomous: true
requirements: [MODL-02, MODL-05]

must_haves:
  truths:
    - "LightGBM point estimator trains on statistical residuals and predicts corrections"
    - "Four quantile regressors train on same feature matrix for CI bounds"
    - "Feature matrix contains residual lags and year normalization per segment"
    - "CV RMSE is computed for LightGBM using expanding-window temporal CV"
  artifacts:
    - path: "src/models/ml/__init__.py"
      provides: "Package marker for ml submodule"
    - path: "src/models/ml/gradient_boost.py"
      provides: "LightGBM point estimator, feature engineering, CV wrapper"
      exports: ["build_residual_features", "fit_lgbm_point", "lgbm_cv_for_segment"]
    - path: "src/models/ml/quantile_models.py"
      provides: "Four quantile regressors for 80% and 95% CI bounds"
      exports: ["fit_lgbm_quantile", "fit_all_quantile_models", "QUANTILE_ALPHAS"]
    - path: "tests/test_ml_models.py"
      provides: "Unit tests for LightGBM fitting, feature engineering, CV, quantile models"
  key_links:
    - from: "src/models/ml/gradient_boost.py"
      to: "data/processed/residuals_statistical.parquet"
      via: "pd.read_parquet in build_residual_features"
      pattern: "read_parquet.*residuals_statistical"
    - from: "src/models/ml/gradient_boost.py"
      to: "src/models/statistical/regression.py"
      via: "temporal_cv_generic reuse"
      pattern: "from src.models.statistical.regression import temporal_cv_generic"
---

<objective>
Train LightGBM point and quantile models on Phase 2 statistical residuals.

Purpose: Build the ML refinement layer (MODL-02) — LightGBM trained on residuals learns what the statistical models missed. Quantile regressors (MODL-05 partial) produce the raw CI bounds that the forecast engine will use.
Output: `src/models/ml/` package with gradient_boost.py and quantile_models.py, plus tests.
</objective>

<execution_context>
@/Users/simonleowegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonleowegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ml-ensemble-and-validation/03-CONTEXT.md
@.planning/phases/03-ml-ensemble-and-validation/03-RESEARCH.md

<interfaces>
<!-- Phase 2 reusable functions the executor needs -->

From src/models/statistical/regression.py:
```python
def temporal_cv_generic(
    series: np.ndarray,
    fit_fn: callable,       # fit_fn(train: np.ndarray) -> fitted_model
    forecast_fn: callable,  # forecast_fn(fitted_model, steps: int) -> np.ndarray
    n_splits: int = 3,
) -> list[dict]:
    """Expanding-window temporal CV. Returns list of dicts with 'rmse' and 'mape' keys."""
```

From src/diagnostics/model_eval.py:
```python
def compute_rmse(actual: np.ndarray, predicted: np.ndarray) -> float: ...
def compute_mape(actual: np.ndarray, predicted: np.ndarray) -> float: ...
```

From config/settings.py:
```python
PROJECT_ROOT = Path(__file__).resolve().parent.parent
DATA_PROCESSED = PROJECT_ROOT / "data" / "processed"
MODELS_DIR = PROJECT_ROOT / "models"
BASE_YEAR = 2020
```

Input contract (residuals_statistical.parquet):
```
Schema: year (int), segment (str), residual (float), model_type (str)
Rows: ~60 (4 segments x 15 years: 2010-2024)
Segments: ["ai_hardware", "ai_infrastructure", "ai_software", "ai_adoption"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependencies and create LightGBM point estimator with feature engineering</name>
  <files>pyproject.toml, uv.lock, src/models/ml/__init__.py, src/models/ml/gradient_boost.py, tests/test_ml_models.py</files>
  <read_first>
    - pyproject.toml
    - src/models/statistical/regression.py
    - src/diagnostics/model_eval.py
    - scripts/run_statistical_pipeline.py
    - config/settings.py
    - data/processed/residuals_statistical.parquet (verify exists)
  </read_first>
  <behavior>
    - TestFeatureEngineering::test_build_residual_features_shape: build_residual_features on 4-segment x 15-year DataFrame produces (4 * 13, 3+) columns (2 lags dropped per segment). Columns include residual_lag1, residual_lag2, year_norm.
    - TestFeatureEngineering::test_lag_values_correct: residual_lag1 for year=2012 segment=X equals the residual for year=2011 segment=X.
    - TestFeatureEngineering::test_no_nan_in_output: output of build_residual_features has zero NaN values.
    - TestLGBMPointModel::test_fit_lgbm_point_returns_model: fit_lgbm_point returns an LGBMRegressor instance with predict method.
    - TestLGBMPointModel::test_fit_lgbm_point_predictions_shape: model.predict(X) returns array of shape (n_samples,).
    - TestLGBMPointModel::test_lgbm_cv_returns_metrics: lgbm_cv_for_segment returns list of dicts each containing "rmse" key with float value > 0.
  </behavior>
  <action>
    1. Run `uv add lightgbm>=4.6.0 shap>=0.46.0` to add both dependencies at once.

    2. Create `src/models/ml/__init__.py` as empty file.

    3. Create `src/models/ml/gradient_boost.py` with:
       - `build_residual_features(residuals_df: pd.DataFrame) -> pd.DataFrame`:
         * Sort by segment, year. GroupBy("segment") shift(1) and shift(2) for residual_lag1, residual_lag2.
         * Add year_norm = (year - 2010) / 14.0.
         * dropna(subset=["residual_lag1", "residual_lag2"]) — only drop rows where lags are NaN, not other columns.
         * Return DataFrame with columns: year, segment, residual, model_type, residual_lag1, residual_lag2, year_norm.
         * FEATURE_COLS = ["residual_lag1", "residual_lag2", "year_norm"] as module-level constant.

       - `fit_lgbm_point(X: np.ndarray, y: np.ndarray) -> lgb.LGBMRegressor`:
         * LGBMRegressor with objective="regression", n_estimators=100, max_depth=3, learning_rate=0.05, num_leaves=7, min_child_samples=3, subsample=0.8, colsample_bytree=0.8, random_state=42, verbose=-1.
         * model.fit(X, y). Return model.

       - `lgbm_cv_for_segment(residual_series: np.ndarray, feature_matrix: np.ndarray, n_splits: int = 3) -> list[dict]`:
         * Use temporal_cv_generic from src.models.statistical.regression.
         * fit_fn wraps LightGBM: receives train_y, computes train_idx = slice(0, len(train_y)), fits model on feature_matrix[train_idx]. Return model.
         * forecast_fn: receives model + steps, predicts on feature_matrix[len(model_train):len(model_train)+steps] where model_train length is tracked. Use a simple approach: predict on the next `steps` rows of feature_matrix after training window.
         * IMPORTANT: Because temporal_cv_generic only passes y slices, the wrapper must align X indices internally. Use a closure over feature_matrix with a mutable index tracker.

    4. Write tests in tests/test_ml_models.py. Use synthetic residuals DataFrame matching the schema (year 2010-2024, 4 segments, float residuals). Do NOT read actual parquet file — create fixtures inline.

    RED phase: Write all tests first, run `uv run pytest tests/test_ml_models.py -x -q` — all must FAIL.
    GREEN phase: Implement gradient_boost.py, run tests — all must PASS.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ml_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def build_residual_features" src/models/ml/gradient_boost.py` returns 1
    - `grep -c "def fit_lgbm_point" src/models/ml/gradient_boost.py` returns 1
    - `grep -c "def lgbm_cv_for_segment" src/models/ml/gradient_boost.py` returns 1
    - `grep "FEATURE_COLS" src/models/ml/gradient_boost.py` matches a list containing "residual_lag1"
    - `grep "objective.*regression" src/models/ml/gradient_boost.py` matches
    - `grep "max_depth=3" src/models/ml/gradient_boost.py` matches
    - `grep "lightgbm" pyproject.toml` matches
    - `grep "shap" pyproject.toml` matches
    - `uv run pytest tests/test_ml_models.py -x -q` exits 0
  </acceptance_criteria>
  <done>LightGBM point estimator trains on residual features, CV wrapper produces RMSE metrics, all 6+ tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build quantile regression models for confidence intervals</name>
  <files>src/models/ml/quantile_models.py, tests/test_ml_models.py</files>
  <read_first>
    - src/models/ml/gradient_boost.py (just created in Task 1)
    - tests/test_ml_models.py (append to existing test file)
  </read_first>
  <behavior>
    - TestQuantileModels::test_fit_lgbm_quantile_returns_model: fit_lgbm_quantile(X, y, alpha=0.10) returns LGBMRegressor.
    - TestQuantileModels::test_quantile_objective_set: model trained with alpha=0.10 has objective="quantile" in get_params().
    - TestQuantileModels::test_fit_all_quantile_models_returns_four: fit_all_quantile_models(X, y) returns dict with 4 keys: ci80_lower, ci80_upper, ci95_lower, ci95_upper.
    - TestQuantileModels::test_all_models_predict: all 4 models in the dict produce predictions of shape (n_samples,).
    - TestQuantileModels::test_quantile_alphas_dict: QUANTILE_ALPHAS maps ci80_lower->0.10, ci80_upper->0.90, ci95_lower->0.025, ci95_upper->0.975.
  </behavior>
  <action>
    1. Create `src/models/ml/quantile_models.py` with:
       - Module-level constant: `QUANTILE_ALPHAS = {"ci80_lower": 0.10, "ci80_upper": 0.90, "ci95_lower": 0.025, "ci95_upper": 0.975}`

       - `fit_lgbm_quantile(X: np.ndarray, y: np.ndarray, alpha: float) -> lgb.LGBMRegressor`:
         * Same hyperparameters as fit_lgbm_point but with objective="quantile" and alpha=alpha.
         * n_estimators=100, max_depth=3, learning_rate=0.05, num_leaves=7, min_child_samples=3, subsample=0.8, random_state=42, verbose=-1.

       - `fit_all_quantile_models(X: np.ndarray, y: np.ndarray) -> dict[str, lgb.LGBMRegressor]`:
         * Iterate QUANTILE_ALPHAS, call fit_lgbm_quantile for each. Return dict mapping key -> fitted model.

    2. Append quantile test classes to tests/test_ml_models.py.

    RED: Write tests, verify they fail.
    GREEN: Implement quantile_models.py, verify all pass.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ml_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def fit_lgbm_quantile" src/models/ml/quantile_models.py` returns 1
    - `grep -c "def fit_all_quantile_models" src/models/ml/quantile_models.py` returns 1
    - `grep 'QUANTILE_ALPHAS' src/models/ml/quantile_models.py` matches a dict with "ci80_lower"
    - `grep 'objective.*quantile' src/models/ml/quantile_models.py` matches
    - `uv run pytest tests/test_ml_models.py -x -q` exits 0
  </acceptance_criteria>
  <done>Four quantile models train on same feature matrix as point model, each with correct alpha. All point + quantile tests pass (11+ tests total).</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_ml_models.py -x -q` — all tests pass
- `uv run python -c "from src.models.ml.gradient_boost import build_residual_features, fit_lgbm_point, lgbm_cv_for_segment; print('OK')"` — imports succeed
- `uv run python -c "from src.models.ml.quantile_models import fit_lgbm_quantile, fit_all_quantile_models, QUANTILE_ALPHAS; print('OK')"` — imports succeed
- `uv run pytest tests/ -q` — full suite still green
</verification>

<success_criteria>
- LightGBM trains on residual features (lags + year_norm) and produces predictions
- CV wrapper reuses temporal_cv_generic and returns per-fold RMSE metrics
- Four quantile models train with correct alpha values for 80%/95% CI
- All tests pass with synthetic data fixtures (no live data dependency)
- lightgbm and shap packages are in pyproject.toml
</success_criteria>

<output>
After completion, create `.planning/phases/03-ml-ensemble-and-validation/03-01-SUMMARY.md`
</output>
