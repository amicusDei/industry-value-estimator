---
phase: 09-ground-up-model-rework-and-value-chain-design
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/industries/ai.yaml
  - src/processing/features.py
  - tests/test_features.py
  - tests/test_config.py
  - tests/test_contract_usd_billions.py
autonomous: true
requirements: [MODL-04]

must_haves:
  truths:
    - "Every EDGAR company in ai.yaml has exactly one value_chain_layer assigned"
    - "A value_chain_layer_taxonomy section exists in ai.yaml with locked_date and 4 layer definitions"
    - "The legacy value_chain multiplier section is commented out in ai.yaml"
    - "build_indicator_matrix returns a flat macro indicator matrix without PCA"
    - "build_pca_composite function no longer exists in features.py"
    - "Contract test file exists asserting USD billions range for forecasts_ensemble.parquet"
  artifacts:
    - path: "config/industries/ai.yaml"
      provides: "value_chain_layer_taxonomy section"
      contains: "value_chain_layer_taxonomy"
    - path: "src/processing/features.py"
      provides: "Flat feature builder without PCA"
      exports: ["build_indicator_matrix", "build_manual_composite", "assess_stationarity"]
    - path: "tests/test_contract_usd_billions.py"
      provides: "Contract tests for USD billions output and CAGR range"
    - path: "tests/test_config.py"
      provides: "Taxonomy validation tests"
  key_links:
    - from: "config/industries/ai.yaml"
      to: "tests/test_config.py"
      via: "pytest assertion that taxonomy section exists and all companies have layers"
      pattern: "value_chain_layer_taxonomy"
    - from: "src/processing/features.py"
      to: "tests/test_features.py"
      via: "TestBuildIndicatorMatrix passes without PCA imports"
      pattern: "build_indicator_matrix"
---

<objective>
Lock the value chain layer taxonomy in ai.yaml, rebuild features.py as a flat feature builder (deleting PCA), and create Wave 0 test scaffolds for the phase.

Purpose: MODL-04 requires taxonomy locked before any attribution. The flat feature builder and contract tests are prerequisites for Plans 09-02 and 09-03.
Output: Locked taxonomy in ai.yaml, PCA-free features.py, contract test file, updated config tests.
</objective>

<execution_context>
@/Users/simonleowegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonleowegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ground-up-model-rework-and-value-chain-design/09-CONTEXT.md
@.planning/phases/09-ground-up-model-rework-and-value-chain-design/09-RESEARCH.md

<interfaces>
<!-- Key signatures the executor needs from existing code -->

From src/processing/features.py (CURRENT — to be modified):
```python
def build_indicator_matrix(
    df: pd.DataFrame,
    indicators: list[str],
    segment: str | None = None,
) -> tuple[np.ndarray, pd.Index]:
    # Currently builds wide indicator matrix — keep this signature
    # DELETE the PCA sklearn imports and build_pca_composite function

def build_pca_composite(matrix: np.ndarray, index: pd.Index) -> pd.Series:
    # DELETE this entire function

def build_manual_composite(matrix: np.ndarray, index: pd.Index, ...) -> pd.Series:
    # PRESERVE unchanged

def assess_stationarity(series: np.ndarray | pd.Series) -> dict:
    # PRESERVE unchanged
```

From config/industries/ai.yaml (existing edgar_companies structure):
```yaml
edgar_companies:
  - name: "NVIDIA Corporation"
    cik: "1045810"
    ticker: "NVDA"
    value_chain_layer: "ai_hardware"
    ai_disclosure_type: "direct"
    primary_ai_segment: "ai_hardware"
    # ... 14 companies total, all have value_chain_layer
```

From config/industries/ai.yaml (existing value_chain section to comment out — lines ~144+):
```yaml
value_chain:
  anchor_year: 2023
  anchor_value_usd_billions: 200
  segment_shares:
    ai_hardware: 0.35
    ai_infrastructure: 0.25
    ai_software: 0.25
    ai_adoption: 0.15
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock value chain taxonomy in ai.yaml and create taxonomy + contract test scaffolds</name>
  <files>config/industries/ai.yaml, tests/test_config.py, tests/test_contract_usd_billions.py</files>
  <read_first>
    - config/industries/ai.yaml
    - tests/test_config.py
    - .planning/phases/09-ground-up-model-rework-and-value-chain-design/09-RESEARCH.md (Pattern 8: Value Chain Taxonomy)
  </read_first>
  <action>
1. In `config/industries/ai.yaml`, add a `value_chain_layer_taxonomy` top-level section BEFORE the existing `value_chain` section. Use this exact structure:

```yaml
value_chain_layer_taxonomy:
  locked_date: "2026-03-24"
  rationale: >
    One-to-one mapping between value chain layers and market segments prevents
    double-counting in Phase 10 attribution. Every company is assigned to exactly
    one primary layer. The primary layer determines which segment's market size
    that company's AI revenue contributes to.
  layers:
    - layer_id: chip
      maps_to_segment: ai_hardware
      description: "GPU, CPU, AI accelerator manufacturers (NVIDIA, AMD, Intel, TSMC)"
    - layer_id: cloud
      maps_to_segment: ai_infrastructure
      description: "Cloud platform AI infrastructure providers (Microsoft/Azure, Alphabet/GCP, Amazon/AWS, Oracle)"
    - layer_id: application
      maps_to_segment: ai_software
      description: "AI software, platform, and SaaS companies (Salesforce, ServiceNow, Palantir, C3.ai)"
    - layer_id: end_market
      maps_to_segment: ai_adoption
      description: "Companies using AI primarily within non-AI products (Meta, IBM, Accenture)"
  multi_layer_policy: >
    Companies that span multiple layers are assigned to the layer where the largest
    share of their AI-specific revenue originates. Secondary layer participation is
    noted in company notes but does not affect segment attribution in Phase 10.
```

2. Comment out the existing `value_chain:` section (lines starting at ~144) by prefixing each line with `# LEGACY: `. Add a comment above: `# LEGACY — archived v1.0 multiplier approach. No longer read by pipeline after Phase 9.`

3. Add `model_version: "v1.1_real_data"` as a top-level key in ai.yaml (near the top, after `name` and `display_name`).

4. In `tests/test_config.py`, append a new test class `TestValueChainTaxonomy`:
   - `test_taxonomy_section_exists`: assert `value_chain_layer_taxonomy` key exists in loaded config
   - `test_taxonomy_has_four_layers`: assert `len(config["value_chain_layer_taxonomy"]["layers"]) == 4`
   - `test_taxonomy_locked_date`: assert `config["value_chain_layer_taxonomy"]["locked_date"] == "2026-03-24"`
   - `test_all_edgar_companies_have_layer`: iterate `config["edgar_companies"]`, assert every company has `value_chain_layer` in `{"ai_hardware", "ai_infrastructure", "ai_software", "ai_adoption"}`
   - `test_layer_segment_mapping_complete`: assert all 4 `maps_to_segment` values are present: `{"ai_hardware", "ai_infrastructure", "ai_software", "ai_adoption"}`
   - `test_model_version_is_v1_1`: assert `config["model_version"] == "v1.1_real_data"`

5. Create `tests/test_contract_usd_billions.py` with these test functions (they will FAIL until Plan 09-03 runs — this is the Wave 0 scaffold):
   - `test_point_estimate_is_usd_billions`: load `forecasts_ensemble.parquet`, assert all `point_estimate_real_2020 > 1.0` and `point_estimate_real_2020 >= 0`
   - `test_total_market_size_plausible`: for historical years 2017-2025 (where `is_forecast == False`), assert total per year > $50B and < $2000B
   - `test_cagr_range`: compute CAGR from 2025 to 2030 per segment, assert each is between 0.15 and 0.60 (15-60% — wider than the 25-40% target to allow some flexibility; the 25-40% check is a documentation/verification concern, not a hard test gate)
   - `test_no_negative_forecasts`: assert no row has `point_estimate_real_2020 < 0`
   - Mark each test with `@pytest.mark.skipif(not (DATA_PROCESSED / "forecasts_ensemble.parquet").exists(), reason="forecasts not yet generated")` so the test suite doesn't fail before Plan 09-03 produces the parquet.
  </action>
  <verify>
    <automated>cd /Users/simonleowegner/my-project && python -c "import yaml; c=yaml.safe_load(open('config/industries/ai.yaml')); assert 'value_chain_layer_taxonomy' in c; assert c['model_version']=='v1.1_real_data'; assert len(c['value_chain_layer_taxonomy']['layers'])==4; print('taxonomy OK')" && pytest tests/test_config.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "value_chain_layer_taxonomy:" config/industries/ai.yaml returns a match
    - grep "locked_date.*2026-03-24" config/industries/ai.yaml returns a match
    - grep "model_version.*v1.1_real_data" config/industries/ai.yaml returns a match
    - grep "# LEGACY" config/industries/ai.yaml returns matches for the old value_chain section
    - grep "TestValueChainTaxonomy" tests/test_config.py returns a match
    - grep "test_point_estimate_is_usd_billions" tests/test_contract_usd_billions.py returns a match
    - grep "test_cagr_range" tests/test_contract_usd_billions.py returns a match
    - pytest tests/test_config.py -x passes (all tests including new taxonomy tests)
  </acceptance_criteria>
  <done>value_chain_layer_taxonomy locked in ai.yaml with 4 layers and locked_date; legacy value_chain commented out; model_version set to v1.1_real_data; 6 new taxonomy tests pass; contract test scaffold exists</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild features.py as flat feature builder — delete PCA path entirely</name>
  <files>src/processing/features.py, tests/test_features.py</files>
  <read_first>
    - src/processing/features.py
    - tests/test_features.py
    - .planning/phases/09-ground-up-model-rework-and-value-chain-design/09-RESEARCH.md (Pattern 1: Flat Feature Builder)
  </read_first>
  <action>
1. In `src/processing/features.py`:
   - Delete `from sklearn.decomposition import PCA` import
   - Delete `from sklearn.pipeline import Pipeline` import
   - Delete `from sklearn.preprocessing import StandardScaler` import
   - Keep `import numpy as np`, `import pandas as pd`, `from statsmodels.tsa.stattools import adfuller, kpss`, `import warnings`
   - Delete the entire `build_pca_composite` function (starts at line 78, ~48 lines)
   - Update the module docstring: remove reference to "PCA reduces these to the first principal component" and replace with "Flat indicator matrix for ARIMA exogenous regressors and LightGBM features"
   - Modify `build_indicator_matrix` to ensure it works as a flat feature builder:
     - Keep exact same signature: `def build_indicator_matrix(df: pd.DataFrame, indicators: list[str], segment: str | None = None) -> tuple[np.ndarray, pd.Index]`
     - Update docstring to: "Build flat macro indicator matrix from long-format processed data. Output: wide matrix (n_years x n_indicators) in value_real_2020 units. No PCA reduction applied."
     - The body should remain functionally the same (pivot wide, select indicators, ffill/bfill, return values and index)
   - Keep `build_manual_composite` and `assess_stationarity` completely unchanged

2. In `tests/test_features.py`:
   - Delete the `TestPcaComposite` class entirely (the function it tests no longer exists)
   - If there are any imports of `build_pca_composite`, delete them
   - Update `TestBuildIndicatorMatrix` docstring if it references PCA
   - Ensure remaining tests still pass — `build_indicator_matrix` signature is unchanged so `TestBuildIndicatorMatrix` should pass without modification
  </action>
  <verify>
    <automated>cd /Users/simonleowegner/my-project && python -c "from src.processing.features import build_indicator_matrix, build_manual_composite, assess_stationarity; print('imports OK')" && python -c "import src.processing.features as f; assert not hasattr(f, 'build_pca_composite'), 'PCA function still exists'; print('PCA deleted OK')" && pytest tests/test_features.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "build_pca_composite" src/processing/features.py returns NO match
    - grep "from sklearn.decomposition import PCA" src/processing/features.py returns NO match
    - grep "from sklearn.pipeline import Pipeline" src/processing/features.py returns NO match
    - grep "build_indicator_matrix" src/processing/features.py returns at least one match (function still exists)
    - grep "build_manual_composite" src/processing/features.py returns at least one match (preserved)
    - grep "assess_stationarity" src/processing/features.py returns at least one match (preserved)
    - grep "TestPcaComposite" tests/test_features.py returns NO match
    - pytest tests/test_features.py -x passes
  </acceptance_criteria>
  <done>features.py is a flat feature builder with no PCA; build_pca_composite deleted; sklearn PCA/Pipeline/StandardScaler imports removed; build_manual_composite and assess_stationarity preserved; all remaining feature tests pass</done>
</task>

</tasks>

<verification>
- `pytest tests/test_config.py tests/test_features.py -x` passes
- `python -c "import yaml; c=yaml.safe_load(open('config/industries/ai.yaml')); assert 'value_chain_layer_taxonomy' in c and c['model_version']=='v1.1_real_data'"` succeeds
- `grep -c "build_pca_composite" src/processing/features.py` returns 0
- `grep -c "value_chain_layer_taxonomy" config/industries/ai.yaml` returns >= 1
</verification>

<success_criteria>
- Value chain taxonomy is locked in ai.yaml with 4 layers and a locked_date
- Legacy value_chain section is commented out as LEGACY
- model_version is set to v1.1_real_data in ai.yaml
- features.py has no PCA code; build_indicator_matrix is a flat feature builder
- Contract test scaffold exists (will run after Plan 09-03)
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/09-ground-up-model-rework-and-value-chain-design/09-01-SUMMARY.md`
</output>
