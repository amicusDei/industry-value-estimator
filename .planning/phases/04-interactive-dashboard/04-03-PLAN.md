---
phase: 04-interactive-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: []
autonomous: false
requirements: [PRES-01, PRES-02, PRES-03, DATA-07]

must_haves:
  truths:
    - "User confirms dashboard loads and displays correctly in browser"
    - "User confirms fan chart CI bands are visually correct"
    - "User confirms all 4 tabs render content"
    - "User confirms source attribution footnotes are visible"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint — user confirms the dashboard renders correctly in a browser.

Purpose: Automated tests verify data and component structure, but visual quality (chart aesthetics, layout alignment, CI band visibility, color contrast) requires human eyes.
Output: User approval or list of visual issues to address.
</objective>

<execution_context>
@/Users/simonleowegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonleowegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-interactive-dashboard/04-02-SUMMARY.md
@.planning/phases/04-interactive-dashboard/04-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of running dashboard</name>
  <files>scripts/run_dashboard.py</files>
  <read_first>
    .planning/phases/04-interactive-dashboard/04-UI-SPEC.md
  </read_first>
  <action>
Launch the dashboard server for user verification:
```bash
cd /Users/simonleowegner/my-project && python scripts/run_dashboard.py
```
Then present the verification checklist to the user.

**What was built:**
Complete Dash dashboard with 4 tabs (Overview, Segments, Drivers, Diagnostics), global segment dropdown, USD toggle, fan charts with CI bands, SHAP attribution image, diagnostics scorecard, backtest chart, and source attribution footnotes on every chart.

**Verification steps for user:**

1. Open http://127.0.0.1:8050 in your browser

2. **Overview tab** (loads by default):
   - Headline stat visible at top with large font
   - Fan chart shows historical solid line + forecast dashed line
   - 80% and 95% CI bands visible as semi-transparent blue fills
   - Vertical dashed line at forecast boundary (2024/2025)
   - Segment breakdown bar chart below
   - Attribution footnotes below each chart

3. **Segments tab:**
   - Click "Segments" tab
   - 2x2 grid of per-segment fan charts
   - Each chart has attribution footnote

4. **Drivers tab:**
   - Click "Drivers" tab
   - SHAP summary PNG image displayed
   - Heading and explanatory text visible
   - Attribution footnote below

5. **Diagnostics tab:**
   - Click "Diagnostics" tab
   - Scorecard table with RMSE values for all 4 segments
   - Backtest residuals chart below
   - Attribution footnotes below each

6. **Global controls:**
   - Change segment dropdown from "All Segments" to "AI Software" — charts update
   - Toggle "Nominal USD" radio — chart y-values change
   - Switch tabs — dropdown and toggle values persist (do NOT reset)

7. **Color and style:**
   - White background with light gray (#F4F6FA) tab content area
   - Deep blue (#1E5AC8) for lines and accents
   - Footer text at bottom
  </action>
  <verify>
    <automated>cd /Users/simonleowegner/my-project && python3 -c "from src.dashboard.app import app; assert app.layout is not None; print('Dashboard layout ready for visual verification')"</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard loads without errors in browser
    - All 4 tabs display content
    - Fan chart shows CI bands (two nested semi-transparent regions)
    - Global controls persist across tab switches
    - Attribution footnotes visible below every chart
    - No broken images or missing content
  </acceptance_criteria>
  <done>User has approved the dashboard visual quality or provided feedback for fixes.</done>
</task>

</tasks>

<verification>
User visual approval of the running dashboard.
</verification>

<success_criteria>
User confirms the dashboard meets the visual and interaction requirements from the UI-SPEC. All 4 tabs render correctly, charts display CI bands, and source attribution is visible.
</success_criteria>

<output>
After completion, create `.planning/phases/04-interactive-dashboard/04-03-SUMMARY.md`
</output>
