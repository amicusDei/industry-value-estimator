---
phase: 06-pipeline-integration-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/statistical/prophet_model.py
  - tests/test_pipeline_wiring.py
autonomous: true
requirements:
  - DATA-05
  - MODL-08
must_haves:
  truths:
    - "fit_prophet_segment accepts a changepoint_year parameter and uses it instead of hardcoded 2022"
    - "run_prophet_cv accepts a changepoint_year parameter and threads it to each fold's Prophet model"
    - "Integration tests exist that verify LSEG scalar loading, break detection, stationarity call, OLS call, and Prophet changepoint threading"
  artifacts:
    - path: "src/models/statistical/prophet_model.py"
      provides: "Configurable changepoint_year parameter on fit_prophet_segment and run_prophet_cv"
      contains: "changepoint_year"
    - path: "tests/test_pipeline_wiring.py"
      provides: "Integration tests for all Phase 6 wiring points"
      exports: ["TestLsegScalar", "TestBreakDetection", "TestStationarityWiring", "TestOlsWiring", "TestProphetChangepoint"]
  key_links:
    - from: "src/models/statistical/prophet_model.py"
      to: "scripts/run_statistical_pipeline.py"
      via: "changepoint_year parameter consumed by pipeline"
      pattern: "changepoint_year"
---

<objective>
Extend Prophet model functions with a configurable changepoint_year parameter (backward-compatible default=2022) and create the integration test scaffold that Plan 02 will wire against.

Purpose: Prophet's hardcoded changepoint must become configurable before the pipeline can pass in a detected break year. Tests must exist before wiring code is written (Nyquist compliance).
Output: Modified prophet_model.py with changepoint_year param, test_pipeline_wiring.py with all integration test classes.
</objective>

<execution_context>
@/Users/simonleowegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonleowegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipeline-integration-wiring/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/models/statistical/prophet_model.py:
```python
def fit_prophet_segment(df: pd.DataFrame, segment: str) -> Prophet:
    # Currently hardcodes changepoints=["2022-01-01"]

def run_prophet_cv(df: pd.DataFrame, segment: str, n_splits: int = 3) -> list[dict]:
    # Currently hardcodes changepoints=["2022-01-01"] inside loop
```

From src/diagnostics/structural_breaks.py:
```python
def run_cusum(series: pd.Series) -> dict:  # returns {"stat", "p_value", "critical_values"}
def run_chow(series: pd.Series, break_idx: int) -> dict:  # returns {"F_stat", "p_value", "break_year"}
def summarize_breaks(cusum_result, chow_result, markov_result=None) -> dict
```

From src/processing/features.py:
```python
def assess_stationarity(series) -> dict:
    # returns {"adf_stationary", "kpss_stationary", "adf_pval", "kpss_pval", "recommendation_d"}
def build_pca_composite(matrix, train_end_idx) -> tuple:
    # returns (scores, explained_variance, pca_model)
```

From src/models/statistical/regression.py:
```python
def fit_top_down_ols_with_upgrade(y, X) -> tuple:
    # returns (final_results, model_type_str, diagnostics_dict)
    # X MUST include constant column (sm.add_constant)
```

From config/settings.py:
```python
DATA_PROCESSED = Path("data/processed")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add changepoint_year parameter to Prophet functions</name>
  <files>src/models/statistical/prophet_model.py</files>
  <read_first>
    - src/models/statistical/prophet_model.py
    - tests/test_models.py
  </read_first>
  <action>
Add a `changepoint_year: int = 2022` parameter to both `fit_prophet_segment` and `run_prophet_cv` in `src/models/statistical/prophet_model.py`. This is a backward-compatible signature extension — all existing callers that omit the parameter get the current behavior (2022).

In `fit_prophet_segment`:
- Change signature to: `def fit_prophet_segment(df: pd.DataFrame, segment: str, changepoint_year: int = 2022) -> Prophet:`
- Replace `changepoints=["2022-01-01"]` with `changepoints=[f"{changepoint_year}-01-01"]`
- Update docstring to document the new parameter

In `run_prophet_cv`:
- Change signature to: `def run_prophet_cv(df: pd.DataFrame, segment: str, n_splits: int = 3, changepoint_year: int = 2022) -> list[dict]:`
- Replace the hardcoded `if 2022 in train_years.values:` logic with `if changepoint_year in train_years.values:`
- Replace `changepoints = ["2022-01-01"]` with `changepoints = [f"{changepoint_year}-01-01"]`
- Update docstring to document the new parameter

CRITICAL (Pitfall 2 from RESEARCH.md): Both functions MUST be updated — if only one is changed, CV will use a different changepoint year than the final fit, producing inconsistent results.

Do NOT change any other function in the file.
  </action>
  <verify>
    <automated>cd /Users/simonleowegner/my-project && uv run pytest tests/test_models.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "changepoint_year" src/models/statistical/prophet_model.py returns >= 4 (2 signatures + 2 usages)
    - grep "def fit_prophet_segment" src/models/statistical/prophet_model.py shows changepoint_year parameter
    - grep "def run_prophet_cv" src/models/statistical/prophet_model.py shows changepoint_year parameter
    - All existing tests in test_models.py pass (backward compatibility confirmed)
  </acceptance_criteria>
  <done>fit_prophet_segment and run_prophet_cv accept changepoint_year with default=2022, all existing tests pass unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test scaffold for all wiring points</name>
  <files>tests/test_pipeline_wiring.py</files>
  <read_first>
    - scripts/run_statistical_pipeline.py
    - tests/test_models.py
    - tests/test_diagnostics.py
    - tests/test_features.py
  </read_first>
  <action>
Create `tests/test_pipeline_wiring.py` with 5 test classes covering every Phase 6 wiring point. Tests should use mocking to avoid requiring real data files. Import paths reference the production modules.

**Class 1: TestLsegScalar**
- `test_load_lseg_scalar_returns_dict`: Mock `pd.read_parquet` to return a DataFrame with columns [year, industry_segment, Revenue] (year=2026, industry_segment="ai_software", Revenue=473e9 for 10 rows). Assert `_load_lseg_scalar()` returns a dict with key "ai_software" and a float value in [0, 1].
- `test_load_lseg_scalar_missing_file`: Assert `_load_lseg_scalar()` returns empty dict when the parquet file does not exist.
- `test_lseg_scalar_applied_to_pca`: Create a synthetic combined DataFrame (15 years, columns matching _SEGMENT_FEATURES["ai_software"]). Call `_build_segment_series(combined, "ai_software", lseg_scalar={"ai_software": 0.5})`. Assert the returned values differ from calling without lseg_scalar (the scalar amplifies scores).

**Class 2: TestBreakDetection**
- `test_run_break_detection_returns_int`: Create a synthetic step-function series (flat 2010-2021, jump at 2022-2024). Call `_run_break_detection(series)`. Assert return is an int.
- `test_run_break_detection_finds_2022`: Same series. Assert detected break year == 2022.
- `test_run_break_detection_fallback`: Create a smooth linear series with no break. Assert function returns 2022 (fallback default).

**Class 3: TestStationarityWiring**
- `test_assess_stationarity_called_in_pipeline`: Use `unittest.mock.patch` on `scripts.run_statistical_pipeline.assess_stationarity` (after it's imported in the pipeline). Run `run_pipeline(use_real_data=False)`. Assert `assess_stationarity` was called at least 4 times (once per segment).

**Class 4: TestOlsWiring**
- `test_ols_called_in_pipeline`: Use `unittest.mock.patch` on `scripts.run_statistical_pipeline.fit_top_down_ols_with_upgrade` (mock returns a dummy (None, "OLS", {"r2": 0.9, "r2_adj": 0.88})). Run `run_pipeline(use_real_data=False)`. Assert the function was called at least 4 times.

**Class 5: TestProphetChangepoint**
- `test_fit_prophet_segment_custom_changepoint`: Create minimal ds/y DataFrame (2010-2024). Call `fit_prophet_segment(df, "ai_software", changepoint_year=2021)`. Assert the model's changepoints list contains a date with year 2021.
- `test_run_prophet_cv_custom_changepoint`: Use mock.patch on Prophet class to inspect changepoints kwarg. Call `run_prophet_cv(df, "ai_software", changepoint_year=2021)`. Assert changepoints passed to Prophet contain "2021".

NOTE: Tests for classes 3 and 4 (stationarity and OLS wiring) will FAIL initially (RED) because `run_statistical_pipeline.py` hasn't been wired yet. This is expected — Plan 02 makes them GREEN. Mark these with `@pytest.mark.xfail(reason="Plan 06-02 wires these into pipeline")` so the test suite stays green.

Use `sys.path` injection at top of test file (same pattern as run_statistical_pipeline.py) if needed for imports.
  </action>
  <verify>
    <automated>cd /Users/simonleowegner/my-project && uv run pytest tests/test_pipeline_wiring.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_pipeline_wiring.py exists
    - grep -c "class Test" tests/test_pipeline_wiring.py returns >= 5
    - grep -c "def test_" tests/test_pipeline_wiring.py returns >= 9
    - uv run pytest tests/test_pipeline_wiring.py passes (xfail tests counted as expected failures)
    - Full suite still green: uv run python -m pytest -q --tb=short
  </acceptance_criteria>
  <done>Integration test scaffold exists with 5 test classes and 9+ test methods, all passing or xfail, full 222+ test suite green</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_models.py tests/test_pipeline_wiring.py -x -q --tb=short` — all pass
2. `grep -c "changepoint_year" src/models/statistical/prophet_model.py` — returns >= 4
3. `uv run python -m pytest -q --tb=short` — full suite green (222+ tests)
</verification>

<success_criteria>
- Prophet functions accept configurable changepoint_year with backward-compatible default
- All existing tests pass unchanged (no regressions)
- Integration test scaffold covers all 5 wiring points for Plan 02
- xfail markers on tests that require Plan 02 wiring (stationarity + OLS call assertions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline-integration-wiring/06-01-SUMMARY.md`
</output>
