---
phase: 02-statistical-baseline
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - docs/ASSUMPTIONS.md
  - tests/test_docs.py
autonomous: true
requirements: [MODL-09, ARCH-04]

must_haves:
  truths:
    - "ASSUMPTIONS.md exists with a practitioner TL;DR summary section and a detailed mathematical appendix"
    - "Every assumption has an 'If this is wrong' sensitivity note explaining impact direction and magnitude"
    - "Data source assumptions, modeling assumptions, and interpretation caveats are all covered"
    - "Stationarity, distributional, parameter choice, cross-validation design, proxy validity, TRBC representativeness, and regime assumptions are all documented"
  artifacts:
    - path: "docs/ASSUMPTIONS.md"
      provides: "Two-tier assumptions document — TL;DR + detailed appendix"
      contains: "## TL;DR"
    - path: "tests/test_docs.py"
      provides: "Automated tests for ASSUMPTIONS.md section completeness"
  key_links:
    - from: "docs/ASSUMPTIONS.md"
      to: "docs/METHODOLOGY.md"
      via: "cross-reference"
      pattern: "METHODOLOGY.md"
    - from: "docs/ASSUMPTIONS.md"
      to: "src/models/statistical/"
      via: "references model choices"
      pattern: "ARIMA|Prophet|Markov"
---

<objective>
Write the comprehensive ASSUMPTIONS.md documenting every modeling decision from Phase 2, with a practitioner TL;DR up front and a detailed mathematical appendix. Add automated tests to verify section completeness.

Purpose: ARCH-04 and MODL-09 require a documented assumptions file. This is the audit trail for the methodology paper (Phase 5) and the defensibility backbone of the entire project. Written after modeling so it documents actual choices, not hypothetical ones.
Output: `docs/ASSUMPTIONS.md` with two-tier structure; updated `tests/test_docs.py` with assumptions validation tests.
</objective>

<execution_context>
@/Users/simonleowegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonleowegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-statistical-baseline/02-CONTEXT.md
@.planning/phases/02-statistical-baseline/02-RESEARCH.md
@.planning/phases/02-statistical-baseline/02-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 SUMMARY: actual modeling decisions made -->
<!-- The executor should read 02-03-SUMMARY.md to get the actual ARIMA orders chosen,
     Prophet configuration used, structural break dates detected, model comparison winners,
     and OLS upgrade decisions — these become the content of ASSUMPTIONS.md -->

From docs/METHODOLOGY.md (existing Phase 1 doc):
- Already documents market boundary, data sources, processing pipeline
- ASSUMPTIONS.md should cross-reference, not duplicate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ASSUMPTIONS.md with two-tier structure documenting all Phase 2 modeling decisions</name>
  <files>docs/ASSUMPTIONS.md</files>
  <read_first>
    - docs/METHODOLOGY.md (existing doc — cross-reference, do not duplicate)
    - .planning/phases/02-statistical-baseline/02-CONTEXT.md (locked decisions on assumptions doc structure and scope)
    - .planning/phases/02-statistical-baseline/02-03-SUMMARY.md (actual modeling outcomes — ARIMA orders, Prophet config, break dates, winners, OLS diagnostics)
    - src/models/statistical/arima.py (actual parameter choices: max_p, max_q, information_criterion)
    - src/models/statistical/prophet_model.py (actual changepoint config)
    - src/diagnostics/structural_breaks.py (Markov switching config, fallback logic)
    - src/processing/features.py (PCA configuration, stationarity test parameters)
    - src/models/statistical/regression.py (OLS upgrade thresholds)
    - config/industries/ai.yaml (segment definitions, proxy indicators)
  </read_first>
  <action>
    Create `docs/ASSUMPTIONS.md` with the following structure. The content MUST reflect the actual modeling decisions from Plan 03 (read the SUMMARY), not hypothetical ones.

    ```markdown
    # Modeling Assumptions

    *Single source of truth for every assumption in the AI Industry Value Estimator statistical baseline.*
    *See [METHODOLOGY.md](METHODOLOGY.md) for data source and processing pipeline documentation.*

    ## TL;DR — Practitioner Summary

    [5-8 bullet points summarizing the most important assumptions a reader needs to know. Each bullet: one assumption + its implication. Example format:]
    - **Assumption:** [What we assume]. **If wrong:** [Impact direction and magnitude].

    Must cover at minimum:
    1. Stationarity assumption and differencing choice
    2. Structural break at 2022 (GenAI surge)
    3. Per-segment modeling (4 segments modeled independently)
    4. Proxy validity (PCA composite as AI activity index)
    5. Small sample size (~15-25 annual observations)
    6. TRBC universe representativeness

    ## Data Source Assumptions

    ### Proxy Validity
    - Why these proxies (R&D/GDP, ICT exports, patents, researchers, high-tech exports) approximate AI market activity
    - PCA first principal component interpretation as "AI market activity index"
    - Explained variance ratio and what the unexplained portion represents
    - **If this is wrong:** [Direction and magnitude of impact on market size estimates]

    ### TRBC Universe Representativeness
    - LSEG TRBC codes capture listed companies only — private AI companies (e.g., OpenAI pre-IPO) excluded
    - Survivorship bias: delisted companies not in current TRBC universe
    - **If this is wrong:** [Likely underestimates market size by X% based on public/private split in AI]

    ### Geographic Coverage
    - 5 regions, 19 economy codes — covers ~85% of global AI activity
    - Missing: emerging AI ecosystems (UAE, Saudi Arabia, Vietnam, Brazil)
    - **If this is wrong:** [Underestimates by Y% primarily in 2025+ forecasts]

    ### Temporal Coverage
    - Annual frequency from 2010 to latest available year
    - Pre-2010 excluded: AI as a distinct industry barely measurable
    - **If this is wrong:** [Limits trend estimation precision; longer history helps ARIMA but pre-2010 data may be structurally different]

    ## Modeling Assumptions

    ### Stationarity
    - ADF and KPSS tests run on each segment — document actual results per segment
    - Differencing order d selected by pmdarima auto_arima (AICc criterion)
    - **If this is wrong:** [Wrong d leads to over/under-differencing; AICc provides correction but formal tests disagree on X segments]

    ### ARIMA Order Selection
    - AICc used instead of AIC (small-N correction, N < 50)
    - max_p=2, max_q=2 enforced to prevent overfitting with ~15-25 observations
    - Actual orders selected per segment: [fill from 02-03-SUMMARY.md]
    - **If this is wrong:** [Higher-order ARIMA may capture more dynamics but risk overfitting; AICc penalty prevents this]

    ### Prophet Configuration
    - Explicit changepoint at 2022-01-01 for GenAI surge
    - changepoint_prior_scale=0.1 (2x default — allows post-break flexibility)
    - n_changepoints effectively 1 (explicit list overrides default 25)
    - yearly/weekly/daily seasonality disabled (annual data)
    - **If this is wrong:** [Default 25 changepoints would fragment trend into noise; 0.1 scale could still overfit on post-2022 uptick]

    ### Structural Break (2022-2024 GenAI Surge)
    - CUSUM test identifies breakpoint date endogenously per segment
    - Chow test confirms statistical significance at the detected date
    - Markov switching (2 regimes, shared variance, shared slope, regime-specific intercept) models the transition
    - Fallback to dummy-variable OLS if Markov fails to converge (documented per segment)
    - Actual break dates per segment: [fill from 02-03-SUMMARY.md]
    - **If this is wrong:** [Ignoring the break inflates residuals and causes Phase 3 ML to learn a parametric shift as if it were a pattern]

    ### Per-Segment Independence
    - Each of 4 segments (hardware, infrastructure, software, adoption) modeled independently
    - No cross-segment spillover effects modeled
    - Aggregation is post-hoc summation
    - **If this is wrong:** [Cross-segment correlations exist (GPU demand drives cloud infrastructure); independent modeling underestimates correlation-driven forecast uncertainty]

    ### Top-Down GDP Share Regression
    - OLS as baseline, upgraded to WLS/GLS based on diagnostic tests
    - Breusch-Pagan threshold p < 0.05 for heteroscedasticity
    - Ljung-Box threshold p < 0.05 for autocorrelation
    - Actual model type used: [fill from 02-03-SUMMARY.md]
    - **If this is wrong:** [OLS with violated assumptions produces inefficient estimates; WLS/GLS correct but may overfit on small N]

    ## Cross-Validation Assumptions

    ### Temporal CV Design
    - Expanding window via sklearn TimeSeriesSplit
    - n_splits chosen based on series length (typically 3-4 for ~15-25 obs)
    - All preprocessing (StandardScaler for PCA) fitted inside training fold only
    - **If this is wrong:** [Fitting scaler on full data leaks future information; reported metrics would be optimistically biased]

    ### Metric Interpretation
    - RMSE and MAPE as primary comparison metrics
    - R² reported but not primary (inflated by trend in time series)
    - AIC/BIC for within-model comparison, not cross-model
    - Ljung-Box for residual whiteness validation
    - **If this is wrong:** [Relying on R² alone would favor any trending model; RMSE/MAPE from CV are the honest metrics]

    ## Interpretation Caveats

    ### Forecast Horizon Uncertainty
    - Statistical baseline forecasts degrade rapidly beyond 2-3 years with ~15 obs training
    - Phase 3 ML correction intended to reduce forecast error
    - Point forecasts without confidence intervals are not defensible
    - **If this is wrong:** [Users may over-interpret 2030 forecasts; confidence intervals from Phase 3 essential]

    ### Model Selection is Per-Segment
    - ARIMA may win on hardware while Prophet wins on software
    - This is a feature, not a bug — different dynamics per segment
    - But it means no single narrative ("we used ARIMA") describes the full model
    - **If this is wrong:** [Forcing one model on all segments sacrifices accuracy for narrative simplicity]

    ### Proxy != Direct Measurement
    - PCA composite index is a proxy for AI market size, not a direct measurement
    - Top-down GDP share regression is an estimate, not an observation
    - The two cross-validate each other but neither is ground truth
    - **If this is wrong:** [All estimates carry proxy uncertainty on top of model uncertainty; total uncertainty is wider than reported model CI]

    ## Mathematical Appendix

    ### ARIMA(p,d,q) Specification
    [LaTeX-style equations for the ARIMA model, differencing operator, likelihood function, AICc formula]

    ### PCA Composite Construction
    [Standardization formula, eigendecomposition, explained variance ratio, composite score formula]

    ### Chow Test Statistic
    [F-statistic formula, degrees of freedom, null hypothesis]

    ### Markov Switching Model
    [Regime transition probability, EM algorithm outline, smoothed probabilities]

    ### AICc Small-Sample Correction
    [AICc = AIC + 2k(k+1)/(n-k-1), why this matters when n < 50]
    ```

    IMPORTANT: Replace all [fill from 02-03-SUMMARY.md] placeholders with actual values from the Plan 03 summary. If the summary does not contain specific values (e.g., exact ARIMA orders), use the source files (arima.py, prophet_model.py) and explain that specific values depend on the data and are logged during model fitting.
  </action>
  <verify>
    <automated>grep -c "If this is wrong" docs/ASSUMPTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/ASSUMPTIONS.md exists
    - docs/ASSUMPTIONS.md contains "## TL;DR"
    - docs/ASSUMPTIONS.md contains "## Data Source Assumptions"
    - docs/ASSUMPTIONS.md contains "## Modeling Assumptions"
    - docs/ASSUMPTIONS.md contains "## Cross-Validation Assumptions"
    - docs/ASSUMPTIONS.md contains "## Interpretation Caveats"
    - docs/ASSUMPTIONS.md contains "## Mathematical Appendix"
    - docs/ASSUMPTIONS.md contains "If this is wrong" at least 10 times
    - docs/ASSUMPTIONS.md contains "ARIMA" (model documentation)
    - docs/ASSUMPTIONS.md contains "Prophet" (model documentation)
    - docs/ASSUMPTIONS.md contains "Markov" (structural break documentation)
    - docs/ASSUMPTIONS.md contains "PCA" (composite index documentation)
    - docs/ASSUMPTIONS.md contains "AICc" (small sample correction)
    - docs/ASSUMPTIONS.md contains "METHODOLOGY.md" (cross-reference)
  </acceptance_criteria>
  <done>ASSUMPTIONS.md has TL;DR + 4 main sections + mathematical appendix. Every assumption has an "If this is wrong" sensitivity note. Cross-references METHODOLOGY.md. Documents actual choices from Phase 2 modeling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add automated tests for ASSUMPTIONS.md section completeness</name>
  <files>tests/test_docs.py</files>
  <read_first>
    - tests/test_docs.py (existing file — has METHODOLOGY.md tests from Phase 1; add ASSUMPTIONS.md tests)
    - docs/ASSUMPTIONS.md (Task 1 output — verify what sections exist)
  </read_first>
  <behavior>
    - test_assumptions_md_exists: docs/ASSUMPTIONS.md exists and is non-empty
    - test_assumptions_tldr: docs/ASSUMPTIONS.md contains "## TL;DR"
    - test_assumptions_data_source_section: contains "## Data Source Assumptions"
    - test_assumptions_modeling_section: contains "## Modeling Assumptions"
    - test_assumptions_cv_section: contains "## Cross-Validation Assumptions"
    - test_assumptions_caveats_section: contains "## Interpretation Caveats"
    - test_assumptions_appendix: contains "## Mathematical Appendix"
    - test_assumptions_sensitivity_notes: "If this is wrong" appears at least 10 times
    - test_assumptions_covers_all_models: contains "ARIMA" AND "Prophet" AND "Markov" AND "PCA"
  </behavior>
  <action>
    Open `tests/test_docs.py` and add a `TestAssumptionsDoc` class below the existing `TestMethodologyDoc` class.

    Each test reads `docs/ASSUMPTIONS.md` using `pathlib.Path` and checks for the presence of required sections and content. Follow the same pattern as the existing METHODOLOGY.md tests.

    ```python
    class TestAssumptionsDoc:
        """ARCH-04, MODL-09: ASSUMPTIONS.md completeness tests."""

        @pytest.fixture
        def assumptions_content(self):
            path = Path(__file__).parent.parent / "docs" / "ASSUMPTIONS.md"
            assert path.exists(), "docs/ASSUMPTIONS.md must exist"
            return path.read_text()

        def test_assumptions_md_exists(self, assumptions_content):
            assert len(assumptions_content) > 500, "ASSUMPTIONS.md too short"

        def test_assumptions_tldr(self, assumptions_content):
            assert "## TL;DR" in assumptions_content

        def test_assumptions_data_source_section(self, assumptions_content):
            assert "## Data Source Assumptions" in assumptions_content

        def test_assumptions_modeling_section(self, assumptions_content):
            assert "## Modeling Assumptions" in assumptions_content

        def test_assumptions_cv_section(self, assumptions_content):
            assert "## Cross-Validation Assumptions" in assumptions_content

        def test_assumptions_caveats_section(self, assumptions_content):
            assert "## Interpretation Caveats" in assumptions_content

        def test_assumptions_appendix(self, assumptions_content):
            assert "## Mathematical Appendix" in assumptions_content

        def test_assumptions_sensitivity_notes(self, assumptions_content):
            count = assumptions_content.count("If this is wrong")
            assert count >= 10, f"Need >= 10 sensitivity notes, found {count}"

        def test_assumptions_covers_all_models(self, assumptions_content):
            for model in ["ARIMA", "Prophet", "Markov", "PCA"]:
                assert model in assumptions_content, f"Missing model: {model}"
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_docs.py contains "TestAssumptionsDoc"
    - tests/test_docs.py contains "test_assumptions_md_exists"
    - tests/test_docs.py contains "test_assumptions_tldr"
    - tests/test_docs.py contains "test_assumptions_sensitivity_notes"
    - tests/test_docs.py contains "test_assumptions_covers_all_models"
    - uv run pytest tests/test_docs.py -x -q exits 0
  </acceptance_criteria>
  <done>ASSUMPTIONS.md passes all 9 automated completeness tests. Existing METHODOLOGY.md tests still pass.</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_docs.py -x -q
uv run pytest tests/ -q (full suite including Phase 1 + Phase 2 tests)
</verification>

<success_criteria>
- docs/ASSUMPTIONS.md exists with TL;DR, Data Source, Modeling, CV, Caveats, and Appendix sections
- Every assumption has "If this is wrong" sensitivity note (>= 10 total)
- All 4 model types documented (ARIMA, Prophet, Markov, PCA)
- AICc small-sample correction documented
- Cross-references METHODOLOGY.md (no duplication)
- 9 automated tests validate section completeness
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-statistical-baseline/02-04-SUMMARY.md`
</output>
