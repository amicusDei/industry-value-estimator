---
phase: 02-statistical-baseline
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/models/statistical/arima.py
  - src/models/statistical/prophet_model.py
  - data/processed/residuals_statistical.parquet
  - tests/test_models.py
autonomous: true
requirements: [MODL-01, MODL-06, MODL-08]

must_haves:
  truths:
    - "ARIMA model fits on each of 4 AI segments with AICc-selected order and produces out-of-sample forecasts"
    - "Prophet model fits on each segment with explicit 2022 changepoint and produces forecasts"
    - "ARIMA and Prophet are compared per segment using temporal CV RMSE/MAPE — winner selected per segment"
    - "Structural break analysis runs on aggregate first then per-segment with segment-specific break dates"
    - "Residuals from the winning model per segment are saved to data/processed/residuals_statistical.parquet with year-aligned index"
    - "Temporal cross-validation uses expanding window with no data leakage"
  artifacts:
    - path: "src/models/statistical/arima.py"
      provides: "select_arima_order, fit_arima_segment, forecast_arima, get_arima_residuals"
      exports: ["select_arima_order", "fit_arima_segment", "forecast_arima", "get_arima_residuals"]
    - path: "src/models/statistical/prophet_model.py"
      provides: "fit_prophet_segment, forecast_prophet, get_prophet_residuals"
      exports: ["fit_prophet_segment", "forecast_prophet", "get_prophet_residuals"]
    - path: "data/processed/residuals_statistical.parquet"
      provides: "Residuals with columns: year, segment, residual, model_type"
    - path: "tests/test_models.py"
      provides: "Unit tests for ARIMA, Prophet, residual schema"
  key_links:
    - from: "src/models/statistical/arima.py"
      to: "pmdarima.auto_arima"
      via: "import"
      pattern: "pm.auto_arima"
    - from: "src/models/statistical/arima.py"
      to: "src/diagnostics/model_eval.py"
      via: "import compute_rmse, compute_mape"
      pattern: "from src.diagnostics.model_eval import"
    - from: "src/models/statistical/prophet_model.py"
      to: "prophet.Prophet"
      via: "import"
      pattern: "from prophet import Prophet"
    - from: "src/models/statistical/arima.py"
      to: "src/models/statistical/regression.py"
      via: "import temporal_cv_generic"
      pattern: "from src.models.statistical.regression import temporal_cv_generic"
    - from: "data/processed/residuals_statistical.parquet"
      to: "Phase 3 ML training"
      via: "file read"
      pattern: "residuals_statistical.parquet"
---

<objective>
Fit ARIMA (via pmdarima auto_arima with AICc) and Prophet (with explicit 2022 changepoint) on each of the 4 AI segments. Compare using temporal cross-validation. Run structural break analysis aggregate-first then per-segment. Save winning model residuals to Parquet for Phase 3 ML training.

Purpose: This is the core econometric modeling plan — produces the statistical baseline forecasts and the residual file that bridges to Phase 3 ML. Every locked decision about model comparison, per-segment fitting, and structural break scope is implemented here.
Output: ARIMA and Prophet modules, residuals_statistical.parquet, model comparison results.
</objective>

<execution_context>
@/Users/simonleowegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonleowegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-statistical-baseline/02-CONTEXT.md
@.planning/phases/02-statistical-baseline/02-RESEARCH.md
@.planning/phases/02-statistical-baseline/02-01-SUMMARY.md
@.planning/phases/02-statistical-baseline/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: diagnostics -->
From src/diagnostics/structural_breaks.py:
- run_cusum(series: pd.Series) -> dict  # {"stat", "p_value", "critical_values"}
- run_chow(series: pd.Series, break_idx: int) -> dict  # {"F_stat", "p_value", "break_year"}
- fit_markov_switching(series: pd.Series) -> dict  # {"model_type", "results", "regimes", "transition_matrix"}
- summarize_breaks(segment_results: dict) -> dict

From src/diagnostics/model_eval.py:
- compute_rmse(actual, predicted) -> float
- compute_mape(actual, predicted) -> float
- compute_r2(actual, predicted) -> float
- compute_aic_bic(residuals, n_params) -> dict  # {"aic", "bic", "aicc"}
- ljung_box_test(residuals, lags) -> dict  # {"statistic", "p_value"}
- compare_models(arima_cv, prophet_cv, segment) -> dict  # {"winner", "margin_pct", ...}

<!-- From Plan 02: features and regression -->
From src/processing/features.py:
- build_indicator_matrix(df, indicators, segment) -> (np.ndarray, pd.Index)
- build_pca_composite(indicator_matrix, train_end_idx) -> (scores, explained, pipe)
- assess_stationarity(series) -> dict  # {"recommendation_d", "adf_pval", ...}

From src/models/statistical/regression.py:
- temporal_cv_generic(series, fit_fn, forecast_fn, n_splits) -> list[dict]

<!-- Phase 1 data contract -->
From config/settings.py:
- DATA_PROCESSED, BASE_YEAR, MODELS_DIR, load_industry_config("ai")

From config/industries/ai.yaml:
- segments: ai_hardware, ai_infrastructure, ai_software, ai_adoption
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ARIMA module — auto order selection, per-segment fitting, forecasting, residuals</name>
  <files>src/models/statistical/arima.py, tests/test_models.py</files>
  <read_first>
    - src/diagnostics/model_eval.py (compute_rmse, compute_mape — used for CV evaluation)
    - src/models/statistical/regression.py (temporal_cv_generic — used for ARIMA CV)
    - src/processing/features.py (assess_stationarity — called before ARIMA)
    - .planning/phases/02-statistical-baseline/02-RESEARCH.md (Pattern 1: ARIMA Per-Segment, Pattern 2: auto_arima AICc, Pitfall 1: AICc vs AIC, Pitfall 5: residual index alignment)
  </read_first>
  <behavior>
    - test_select_arima_order: select_arima_order on a 30-element trended series returns tuple of 3 ints (p, d, q) where all are <= 2
    - test_arima_fits: fit_arima_segment on a synthetic 20-element series with order=(1,1,0) returns an object with .resid attribute of length close to 20
    - test_forecast_arima: forecast_arima on a fitted ARIMA result with steps=5 returns DataFrame with columns containing "mean" and length 5
    - test_arima_residuals_indexed: get_arima_residuals(fitted_result, original_index) returns pd.Series with same index as original_index (year-aligned, not positional)
    - test_arima_cv: run_arima_cv on a 25-element series with n_splits=3 returns list of 3 dicts each with "rmse" and "mape" keys
  </behavior>
  <action>
    Create `src/models/statistical/arima.py` with:

    **select_arima_order(series: pd.Series) -> tuple[int, int, int]:**
    - `import pmdarima as pm`
    - model = pm.auto_arima(series, information_criterion="aicc", stepwise=True, max_p=2, max_q=2, d=None, seasonal=False, error_action="ignore", suppress_warnings=True)
    - Return model.order

    **fit_arima_segment(series: pd.Series, order: tuple[int, int, int]) -> object:**
    - `from statsmodels.tsa.arima.model import ARIMA`
    - model = ARIMA(series, order=order)
    - Return model.fit()

    **forecast_arima(results, steps: int, alpha: float = 0.05) -> pd.DataFrame:**
    - Return results.get_forecast(steps=steps).summary_frame(alpha=alpha)

    **get_arima_residuals(results, original_index: pd.Index) -> pd.Series:**
    - resid = results.resid
    - resid.index = original_index[:len(resid)]  # Align to year index, NOT positional
    - Return resid

    **run_arima_cv(series: pd.Series, order: tuple, n_splits: int = 3) -> list[dict]:**
    - Use temporal_cv_generic with:
      - fit_fn = lambda train: ARIMA(train, order=order).fit()
      - forecast_fn = lambda fitted, steps: fitted.get_forecast(steps=steps).predicted_mean.values
    - Return fold results

    Create `tests/test_models.py` with TestARIMA class. Use synthetic series: np.cumsum(np.random.randn(25)) + np.arange(25) * 0.5 for a trending series with noise.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/models/statistical/arima.py contains "def select_arima_order("
    - src/models/statistical/arima.py contains "def fit_arima_segment("
    - src/models/statistical/arima.py contains "def forecast_arima("
    - src/models/statistical/arima.py contains "def get_arima_residuals("
    - src/models/statistical/arima.py contains "def run_arima_cv("
    - src/models/statistical/arima.py contains 'information_criterion="aicc"'
    - src/models/statistical/arima.py contains "max_p=2, max_q=2"
    - src/models/statistical/arima.py contains "original_index" (residual alignment)
    - tests/test_models.py contains "test_select_arima_order" or "TestARIMA"
    - tests/test_models.py contains "test_arima_residuals_indexed"
    - uv run pytest tests/test_models.py -x -q exits 0
  </acceptance_criteria>
  <done>ARIMA order selection uses AICc with max_p=2, max_q=2. Fitting, forecasting, and residual extraction all work. Residuals are year-indexed (not positional). CV produces fold metrics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prophet module and residual Parquet output with model comparison</name>
  <files>src/models/statistical/prophet_model.py, tests/test_models.py</files>
  <read_first>
    - src/models/statistical/arima.py (Task 1 output — follow same patterns for Prophet)
    - src/diagnostics/model_eval.py (compare_models function)
    - src/models/statistical/regression.py (temporal_cv_generic)
    - .planning/phases/02-statistical-baseline/02-RESEARCH.md (Pattern 3: Prophet with 2022 changepoint, Pitfall 2: default changepoints, Code Example: Saving Residuals, Code Example: Model Comparison)
  </read_first>
  <behavior>
    - test_prophet_fits: fit_prophet_segment on a synthetic 15-year DataFrame (2010-2024) returns a Prophet model object
    - test_prophet_forecast: forecast_prophet on a fitted model with periods=5 returns DataFrame with "yhat" column and 5 rows
    - test_prophet_residuals: get_prophet_residuals returns pd.Series indexed by year integers (not datetime)
    - test_prophet_cv: run_prophet_cv on a 15-year series with n_splits=3 returns list of 3 dicts with "rmse" key
    - test_residuals_schema: save_all_residuals produces a Parquet file with columns ["year", "segment", "residual", "model_type"] and correct dtypes
    - test_residuals_year_alignment: saved residuals year column starts at the first year of the input series (not 0 or some offset)
  </behavior>
  <action>
    Create `src/models/statistical/prophet_model.py` with:

    **fit_prophet_segment(df: pd.DataFrame, segment: str) -> Prophet:**
    - Filter df by industry_segment == segment
    - Aggregate: groupby("year")["value_real_2020"].sum().reset_index()
    - Rename columns: year -> ds, value_real_2020 -> y
    - Convert ds to datetime: pd.to_datetime(ds.astype(str) + "-01-01")
    - Prophet(changepoints=["2022-01-01"], changepoint_prior_scale=0.1, yearly_seasonality=False, weekly_seasonality=False, daily_seasonality=False)
    - model.fit(seg)
    - Return model

    **forecast_prophet(model: Prophet, periods: int) -> pd.DataFrame:**
    - future = model.make_future_dataframe(periods=periods, freq="YS")
    - Return model.predict(future)

    **get_prophet_residuals(model: Prophet, df_segment: pd.DataFrame) -> pd.Series:**
    - in_sample = model.predict(model.history)
    - residuals = df_segment["y"].values - in_sample["yhat"].values
    - Return pd.Series(residuals, index=df_segment["ds"].dt.year.values, name="residual")

    **run_prophet_cv(df: pd.DataFrame, segment: str, n_splits: int = 3) -> list[dict]:**
    - Build segment series, then use TimeSeriesSplit manually (not Prophet's built-in cross_validation — per RESEARCH.md Open Question 3)
    - For each fold: refit Prophet on training period, predict test period, compute RMSE/MAPE
    - Return fold results

    **save_all_residuals(segment_residuals: dict[str, tuple[pd.Series, str]], output_path: str):**
    - segment_residuals maps segment_name -> (residual_series, model_type_string)
    - Concatenate all segments into one DataFrame with columns: year (int), segment (str), residual (float), model_type (str)
    - Write to Parquet via pyarrow with snappy compression
    - Validate: assert no NaN in year column, assert all segments present

    Add TestProphet and TestResiduals classes to tests/test_models.py. For Prophet tests, suppress cmdstanpy logging: `import logging; logging.getLogger("cmdstanpy").setLevel(logging.WARNING)`. Use synthetic data that mimics annual AI segment data (15 years, trending upward with a level shift at 2022).
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/models/statistical/prophet_model.py contains "def fit_prophet_segment("
    - src/models/statistical/prophet_model.py contains "def forecast_prophet("
    - src/models/statistical/prophet_model.py contains "def get_prophet_residuals("
    - src/models/statistical/prophet_model.py contains "def save_all_residuals("
    - src/models/statistical/prophet_model.py contains 'changepoints=["2022-01-01"]'
    - src/models/statistical/prophet_model.py contains "changepoint_prior_scale=0.1"
    - src/models/statistical/prophet_model.py contains "yearly_seasonality=False"
    - tests/test_models.py contains "test_prophet_fits" or "TestProphet"
    - tests/test_models.py contains "test_residuals_schema" or "TestResiduals"
    - tests/test_models.py contains "test_residuals_year_alignment"
    - uv run pytest tests/test_models.py -x -q exits 0
  </acceptance_criteria>
  <done>Prophet fits with explicit 2022 changepoint, forecasts work, residuals are year-indexed. Residual Parquet has correct schema (year, segment, residual, model_type). Model comparison selects winner per segment by CV RMSE.</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_models.py tests/test_diagnostics.py tests/test_features.py -x -q
uv run pytest tests/ -q (full suite)
</verification>

<success_criteria>
- ARIMA fits per segment with AICc order selection (max_p=2, max_q=2)
- Prophet fits per segment with explicit 2022 changepoint and constrained changepoint_prior_scale=0.1
- Temporal CV compares ARIMA vs Prophet per segment; winner selected by mean CV RMSE
- Residuals saved to data/processed/residuals_statistical.parquet with year-aligned index (not positional)
- Residual Parquet schema: year (int), segment (str), residual (float), model_type (str)
- All tests pass, full suite no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-statistical-baseline/02-03-SUMMARY.md`
</output>
