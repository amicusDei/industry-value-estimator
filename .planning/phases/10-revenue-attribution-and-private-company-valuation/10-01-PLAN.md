---
phase: 10-revenue-attribution-and-private-company-valuation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/processing/validate.py
  - config/industries/ai.yaml
  - data/raw/attribution/ai_attribution_registry.yaml
  - data/raw/private_companies/ai_private_registry.yaml
  - tests/test_revenue_attribution.py
  - tests/test_private_valuations.py
  - tests/test_backtesting.py
autonomous: true
requirements: [MODL-02, MODL-03, MODL-06]

must_haves:
  truths:
    - "ATTRIBUTION_SCHEMA and PRIVATE_VALUATION_SCHEMA exist in validate.py and can validate sample DataFrames"
    - "attribution_subsegment_ratios section exists in ai.yaml with ratios for multi-layer companies"
    - "Stub YAML registries exist with 3+ entries each for attribution and private companies"
    - "Test scaffolds exist for all three Phase 10 modules with failing tests (Wave 0)"
  artifacts:
    - path: "src/processing/validate.py"
      provides: "ATTRIBUTION_SCHEMA and PRIVATE_VALUATION_SCHEMA pandera schemas"
      contains: "ATTRIBUTION_SCHEMA"
    - path: "config/industries/ai.yaml"
      provides: "attribution_subsegment_ratios section with per-company layer splits"
      contains: "attribution_subsegment_ratios"
    - path: "data/raw/attribution/ai_attribution_registry.yaml"
      provides: "Stub attribution registry with 3+ entries"
      contains: "entries"
    - path: "data/raw/private_companies/ai_private_registry.yaml"
      provides: "Stub private company registry with 3+ entries"
      contains: "entries"
    - path: "tests/test_revenue_attribution.py"
      provides: "Test scaffold for MODL-02"
    - path: "tests/test_private_valuations.py"
      provides: "Test scaffold for MODL-03"
    - path: "tests/test_backtesting.py"
      provides: "Test scaffold for MODL-06"
  key_links:
    - from: "tests/test_revenue_attribution.py"
      to: "src/processing/validate.py"
      via: "imports ATTRIBUTION_SCHEMA"
      pattern: "from src.processing.validate import ATTRIBUTION_SCHEMA"
    - from: "tests/test_private_valuations.py"
      to: "src/processing/validate.py"
      via: "imports PRIVATE_VALUATION_SCHEMA"
      pattern: "from src.processing.validate import PRIVATE_VALUATION_SCHEMA"
---

<objective>
Create the interface contracts, pandera schemas, config extensions, stub YAML registries, and test scaffolds that Plans 10-02, 10-03, and 10-04 implement against.

Purpose: Wave 0 foundation — every downstream plan has schemas to validate against, YAML stubs for test fixtures, and failing tests to turn green. Prevents the "scavenger hunt" anti-pattern.
Output: Two new pandera schemas in validate.py, attribution_subsegment_ratios in ai.yaml, two stub YAML registries, three test scaffold files.
</objective>

<execution_context>
@/Users/simonleowegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonleowegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-revenue-attribution-and-private-company-valuation/10-CONTEXT.md
@.planning/phases/10-revenue-attribution-and-private-company-valuation/10-RESEARCH.md

<interfaces>
<!-- Existing schemas in validate.py that new schemas must follow the same pattern as -->
From src/processing/validate.py:
```python
EDGAR_RAW_SCHEMA = DataFrameSchema(
    {
        "cik": Column(str, nullable=False),
        "company_name": Column(str, nullable=False),
        "period_end": Column(str, nullable=False),
        "form_type": Column(str, Check.isin(["10-K", "10-Q", "20-F"])),
        "xbrl_concept": Column(str, nullable=False),
        "value_usd": Column(float, nullable=True),
        "bundled_flag": Column(bool, nullable=False),
        "value_chain_layer": Column(str, Check.isin(["ai_hardware", "ai_infrastructure", "ai_software", "ai_adoption"])),
    },
    coerce=True,
    strict=False,
)
```

From src/ingestion/edgar.py:
```python
BUNDLED_SEGMENT_COMPANIES = {
    "0000789019",  # Microsoft
    "0001018724",  # Amazon
    "0001652044",  # Alphabet
    "0001326801",  # Meta
    "0000051143",  # IBM
    "0001281761",  # Accenture
}
```

From config/industries/ai.yaml (edgar_companies with value_chain_layer assignments):
- 14 companies across 4 layers: chip (NVDA, AMD, TSM, INTC), cloud (MSFT, GOOGL, AMZN, ORCL), application (CRM, NOW, PLTR, AI), end_market (META, IBM, ACN)
- value_chain_layer_taxonomy locked 2026-03-24 with layers: chip, cloud, application, end_market
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pandera schemas and attribution_subsegment_ratios to ai.yaml</name>
  <files>src/processing/validate.py, config/industries/ai.yaml</files>
  <read_first>
    - src/processing/validate.py (full file — understand existing schema patterns)
    - config/industries/ai.yaml (full file — find value_chain_layer_taxonomy and edgar_companies sections)
    - src/ingestion/edgar.py lines 30-45 (BUNDLED_SEGMENT_COMPANIES set)
  </read_first>
  <behavior>
    - ATTRIBUTION_SCHEMA validates a DataFrame with columns: company_name (str, not null), cik (str, not null), value_chain_layer (str, one of chip/cloud/application/end_market), attribution_method (str, one of direct_disclosure/management_commentary/analogue_ratio), ai_revenue_usd_billions (float >= 0, not null), uncertainty_low (float >= 0, not null), uncertainty_high (float > 0, not null), vintage_date (str, not null), ratio_source (str, not null), segment (str, one of ai_hardware/ai_infrastructure/ai_software/ai_adoption), year (int, 2017-2026)
    - PRIVATE_VALUATION_SCHEMA validates a DataFrame with columns: company_name (str, not null), confidence_tier (str, one of HIGH/MEDIUM/LOW), implied_ev_low (float > 0), implied_ev_mid (float > 0), implied_ev_high (float > 0), segment (str, one of ai_hardware/ai_infrastructure/ai_software/ai_adoption), vintage_date (str, not null), comparable_mid_multiple (float, 1.0-300.0)
    - ATTRIBUTION_SCHEMA rejects rows where uncertainty_high <= 0
    - PRIVATE_VALUATION_SCHEMA rejects rows where confidence_tier is "INVALID"
  </behavior>
  <action>
    1. Append ATTRIBUTION_SCHEMA and PRIVATE_VALUATION_SCHEMA to the end of src/processing/validate.py, following the exact pattern from 10-RESEARCH.md Code Examples section. Use the same import style (pandera.pandas as pa, Column, DataFrameSchema, Check — already imported at top of file).

    2. Add `attribution_subsegment_ratios` section to config/industries/ai.yaml AFTER the edgar_companies section. Include entries for multi-layer companies:
       - NVIDIA (CIK 0001045810): primary_layer chip, splits: ai_hardware 0.80, ai_infrastructure 0.20, rationale "~80% direct silicon, ~20% data center/infrastructure builds", vintage_date "2026-01"
       - Microsoft (CIK 0000789019): primary_layer cloud, splits: ai_infrastructure 0.85, ai_software 0.15, rationale "Azure AI ~85% infrastructure, ~15% Copilot software", vintage_date "2025-07"
       - Alphabet (CIK 0001652044): primary_layer cloud, splits: ai_infrastructure 0.90, ai_software 0.10, rationale "Google Cloud AI ~90% infrastructure", vintage_date "2025-07"
       - Amazon (CIK 0001018724): primary_layer cloud, splits: ai_infrastructure 0.90, ai_software 0.10, rationale "AWS AI/ML ~90% infrastructure", vintage_date "2025-07"
       - Meta (CIK 0001326801): primary_layer end_market, splits: ai_adoption 0.85, ai_software 0.15, rationale "Ad-driven AI ~85% adoption, ~15% AI platform tools", vintage_date "2025-07"

    3. Note on Salesforce vs Accenture: BUNDLED_SEGMENT_COMPANIES in edgar.py has Accenture (0001281761) but CONTEXT.md lists Salesforce. Resolution: Keep Accenture in BUNDLED_SEGMENT_COMPANIES (AI consulting revenue $3B+ is material) AND add Salesforce (CIK 0001108524) to BUNDLED_SEGMENT_COMPANIES. Update edgar.py to include both. This means 7 bundled companies, which is fine — the YAML attribution registry will cover all of them.
  </action>
  <verify>
    <automated>uv run python -c "from src.processing.validate import ATTRIBUTION_SCHEMA, PRIVATE_VALUATION_SCHEMA; import pandas as pd; df = pd.DataFrame({'company_name':['test'],'cik':['0001'],'value_chain_layer':['chip'],'attribution_method':['direct_disclosure'],'ai_revenue_usd_billions':[1.0],'uncertainty_low':[0.8],'uncertainty_high':[1.2],'vintage_date':['2024-01'],'ratio_source':['test'],'segment':['ai_hardware'],'year':[2024]}); ATTRIBUTION_SCHEMA.validate(df); print('ATTRIBUTION_SCHEMA OK'); df2 = pd.DataFrame({'company_name':['test'],'confidence_tier':['HIGH'],'implied_ev_low':[10.0],'implied_ev_mid':[15.0],'implied_ev_high':[20.0],'segment':['ai_software'],'vintage_date':['2024-01'],'comparable_mid_multiple':[33.0]}); PRIVATE_VALUATION_SCHEMA.validate(df2); print('PRIVATE_VALUATION_SCHEMA OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ATTRIBUTION_SCHEMA" src/processing/validate.py` returns >= 1
    - `grep -c "PRIVATE_VALUATION_SCHEMA" src/processing/validate.py` returns >= 1
    - `grep -c "attribution_subsegment_ratios" config/industries/ai.yaml` returns >= 1
    - `grep -c "0001108524" src/ingestion/edgar.py` returns >= 1 (Salesforce added to BUNDLED_SEGMENT_COMPANIES)
    - The python one-liner in verify passes without error
  </acceptance_criteria>
  <done>ATTRIBUTION_SCHEMA and PRIVATE_VALUATION_SCHEMA validate sample DataFrames; ai.yaml has attribution_subsegment_ratios with 5 multi-layer company entries; BUNDLED_SEGMENT_COMPANIES includes both Accenture and Salesforce (7 companies)</done>
</task>

<task type="auto">
  <name>Task 2: Create stub YAML registries and test scaffolds (Wave 0)</name>
  <files>data/raw/attribution/ai_attribution_registry.yaml, data/raw/private_companies/ai_private_registry.yaml, tests/test_revenue_attribution.py, tests/test_private_valuations.py, tests/test_backtesting.py</files>
  <read_first>
    - data/raw/market_anchors/ai_analyst_registry.yaml (first 50 lines — YAML registry format to follow)
    - tests/test_validate.py (first 50 lines — test class pattern to follow)
    - tests/test_market_anchors.py (first 50 lines — test fixture loading pattern)
    - src/processing/validate.py (newly updated — confirm ATTRIBUTION_SCHEMA and PRIVATE_VALUATION_SCHEMA exist)
  </read_first>
  <action>
    1. Create `data/raw/attribution/ai_attribution_registry.yaml` with 3 stub entries following the exact schema from 10-RESEARCH.md Pattern 1:
       - NVIDIA (CIK 0001045810): ai_revenue_usd_billions 47.5 (Data Center FY2025), attribution_method "direct_disclosure", ratio_source "NVIDIA FY2025 10-K, Data Center segment", vintage_date "2025-02-26", uncertainty_low 45.0, uncertainty_high 50.0, segment "ai_hardware", value_chain_layer "chip", year 2024, estimated_flag false
       - Microsoft (CIK 0000789019): ai_revenue_usd_billions 13.0 (AI run rate), attribution_method "management_commentary", ratio_source "Microsoft FY2024 Q4 earnings call (Satya Nadella)", vintage_date "2024-07-30", uncertainty_low 11.0, uncertainty_high 16.0, segment "ai_infrastructure", value_chain_layer "cloud", year 2024, estimated_flag false
       - Palantir (CIK 0001321655): ai_revenue_usd_billions 2.87 (full revenue = AI), attribution_method "direct_disclosure", ratio_source "Palantir FY2024 10-K", vintage_date "2025-02-05", uncertainty_low 2.73, uncertainty_high 3.01, segment "ai_software", value_chain_layer "application", year 2024, estimated_flag false

    2. Create `data/raw/private_companies/ai_private_registry.yaml` with 3 stub entries following 10-RESEARCH.md Pattern 1:
       - OpenAI: confidence_tier HIGH, last_funding_valuation_usd_billions 157.0, funding_date "2024-10-01", estimated_arr_usd_billions 3.4, arr_source "The Information, 2024-09", comparable_low_multiple 25.0, comparable_mid_multiple 40.0, comparable_high_multiple 60.0, comparable_peer_group "ai_software_pure_play", implied_ev_low 85.0, implied_ev_mid 136.0, implied_ev_high 204.0, segment "ai_software", vintage_date "2024-10-01"
       - Anthropic: confidence_tier HIGH, last_funding_valuation_usd_billions 61.5, funding_date "2024-12-01", estimated_arr_usd_billions 0.9, arr_source "Reuters, 2024-10", comparable_low_multiple 30.0, comparable_mid_multiple 50.0, comparable_high_multiple 70.0, comparable_peer_group "ai_software_pure_play", implied_ev_low 27.0, implied_ev_mid 45.0, implied_ev_high 63.0, segment "ai_software", vintage_date "2024-12-01"
       - Databricks: confidence_tier MEDIUM, last_funding_valuation_usd_billions 43.0, funding_date "2023-09-14", estimated_arr_usd_billions 1.6, arr_source "Forbes, 2024-02", comparable_low_multiple 15.0, comparable_mid_multiple 25.0, comparable_high_multiple 35.0, comparable_peer_group "data_platform", implied_ev_low 24.0, implied_ev_mid 40.0, implied_ev_high 56.0, segment "ai_infrastructure", vintage_date "2024-02-01"

    3. Create `tests/test_revenue_attribution.py` with test class TestAttributionRegistry:
       - test_stub_yaml_loads: Load ai_attribution_registry.yaml, assert 3+ entries, assert all required columns present
       - test_schema_validates_stub: Load YAML into DataFrame, run ATTRIBUTION_SCHEMA.validate(), assert no error
       - test_schema_rejects_missing_vintage: Create DataFrame missing vintage_date, assert SchemaError
       - test_compile_attribution (marked @pytest.mark.skip with reason="Plan 10-02 implementation"): placeholder for compilation test

    4. Create `tests/test_private_valuations.py` with test class TestPrivateValuationRegistry:
       - test_stub_yaml_loads: Load ai_private_registry.yaml, assert 3+ entries
       - test_schema_validates_stub: Load YAML into DataFrame, run PRIVATE_VALUATION_SCHEMA.validate(), assert no error
       - test_ev_ordering: Assert implied_ev_low <= implied_ev_mid <= implied_ev_high for all entries
       - test_compile_private_valuations (marked @pytest.mark.skip with reason="Plan 10-03 implementation"): placeholder

    5. Create `tests/test_backtesting.py` with test class TestBacktesting:
       - test_hard_actuals_source: placeholder test verifying only direct-disclosure companies contribute hard actuals (@pytest.mark.skip reason="Plan 10-04 implementation")
       - test_actual_type_labels: placeholder for actual_type column validation (@pytest.mark.skip)
       - test_parquet_schema: placeholder for backtesting_results.parquet schema (@pytest.mark.skip)
       - test_fold_count: placeholder asserting 3 evaluation folds (@pytest.mark.skip)

    All test files must use `from pathlib import Path`, `import pandas as pd`, `import pytest`, `import yaml`, and follow the existing test patterns in the codebase.
  </action>
  <verify>
    <automated>uv run pytest tests/test_revenue_attribution.py tests/test_private_valuations.py tests/test_backtesting.py -v --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l < data/raw/attribution/ai_attribution_registry.yaml` returns >= 20 (3 entries with all fields)
    - `wc -l < data/raw/private_companies/ai_private_registry.yaml` returns >= 20 (3 entries with all fields)
    - `uv run pytest tests/test_revenue_attribution.py -v` shows at least 2 passed tests (stub load + schema validate)
    - `uv run pytest tests/test_private_valuations.py -v` shows at least 2 passed tests (stub load + schema validate)
    - `uv run pytest tests/test_backtesting.py -v` shows all tests skipped (all are Plan 10-04 placeholders)
    - `grep -c "pytest.mark.skip" tests/test_backtesting.py` returns >= 3
  </acceptance_criteria>
  <done>Stub YAML registries exist with 3+ entries each; test scaffolds for attribution, private valuations, and backtesting exist; non-skipped tests pass; skipped tests are clearly labeled for their downstream plans</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_revenue_attribution.py tests/test_private_valuations.py tests/test_backtesting.py -v --timeout=30` — non-skipped tests pass, skipped tests labeled
- `uv run python -c "from src.processing.validate import ATTRIBUTION_SCHEMA, PRIVATE_VALUATION_SCHEMA; print('OK')"` — schemas importable
- `grep "attribution_subsegment_ratios" config/industries/ai.yaml` — config section exists
</verification>

<success_criteria>
- ATTRIBUTION_SCHEMA and PRIVATE_VALUATION_SCHEMA are importable from validate.py and validate sample data
- ai.yaml has attribution_subsegment_ratios with 5 multi-layer company entries
- BUNDLED_SEGMENT_COMPANIES in edgar.py includes both Accenture and Salesforce
- Stub YAML registries have 3+ entries each with all required fields
- Test scaffolds exist for all 3 downstream modules
- All non-skipped tests pass; all skipped tests reference their downstream plan
</success_criteria>

<output>
After completion, create `.planning/phases/10-revenue-attribution-and-private-company-valuation/10-01-SUMMARY.md`
</output>
