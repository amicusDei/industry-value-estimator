---
phase: 10-revenue-attribution-and-private-company-valuation
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - data/raw/private_companies/ai_private_registry.yaml
  - src/processing/private_valuations.py
  - src/ingestion/pipeline.py
  - tests/test_private_valuations.py
autonomous: true
requirements: [MODL-03]

must_haves:
  truths:
    - "private_valuations_ai.parquet contains 15-20 private AI companies with low/mid/high valuations"
    - "Every company has a confidence tier (HIGH/MEDIUM/LOW) and vintage_date"
    - "Comparable multiples are per-company, not a single global multiple"
    - "Compiled DataFrame passes PRIVATE_VALUATION_SCHEMA validation"
    - "implied_ev_low <= implied_ev_mid <= implied_ev_high for every row"
  artifacts:
    - path: "data/raw/private_companies/ai_private_registry.yaml"
      provides: "Full YAML registry with 15-20 private company entries"
      min_lines: 250
    - path: "src/processing/private_valuations.py"
      provides: "load_private_registry, apply_comparable_multiples, compile_and_write_private_valuations functions"
      exports: ["load_private_registry", "apply_comparable_multiples", "compile_and_write_private_valuations"]
    - path: "data/processed/private_valuations_ai.parquet"
      provides: "Compiled private company valuations"
  key_links:
    - from: "src/processing/private_valuations.py"
      to: "src/processing/validate.py"
      via: "PRIVATE_VALUATION_SCHEMA validation before Parquet write"
      pattern: "PRIVATE_VALUATION_SCHEMA.validate"
    - from: "src/processing/private_valuations.py"
      to: "data/raw/private_companies/ai_private_registry.yaml"
      via: "YAML loading"
      pattern: "yaml.safe_load"
    - from: "src/ingestion/pipeline.py"
      to: "src/processing/private_valuations.py"
      via: "Step 9 import in run_full_pipeline"
      pattern: "from src.processing.private_valuations import compile_and_write_private_valuations"
---

<objective>
Build the private company valuation registry and pipeline: expand YAML to 15-20 companies, implement private_valuations.py with comparable multiple application, wire into pipeline.py as Step 9, produce private_valuations_ai.parquet.

Purpose: MODL-03 — every major private AI company gets a valuation with explicit uncertainty bands and confidence tier. Comparable multiples only (no DCF, per user decision).
Output: Full private company YAML registry (15-20 companies), private_valuations.py module, private_valuations_ai.parquet, pipeline wiring.
</objective>

<execution_context>
@/Users/simonleowegner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonleowegner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-revenue-attribution-and-private-company-valuation/10-CONTEXT.md
@.planning/phases/10-revenue-attribution-and-private-company-valuation/10-RESEARCH.md
@.planning/phases/10-revenue-attribution-and-private-company-valuation/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 10-01 outputs -->
From src/processing/validate.py (added in Plan 10-01):
```python
PRIVATE_VALUATION_SCHEMA = DataFrameSchema(
    {
        "company_name": Column(str, nullable=False),
        "confidence_tier": Column(str, Check.isin(["HIGH", "MEDIUM", "LOW"])),
        "implied_ev_low": Column(float, Check.greater_than(0)),
        "implied_ev_mid": Column(float, Check.greater_than(0)),
        "implied_ev_high": Column(float, Check.greater_than(0)),
        "segment": Column(str, Check.isin(["ai_hardware", "ai_infrastructure", "ai_software", "ai_adoption"])),
        "vintage_date": Column(str, nullable=False),
        "comparable_mid_multiple": Column(float, Check.in_range(1.0, 300.0)),
    },
    coerce=True,
    strict=False,
)
```

From src/ingestion/market_anchors.py (pattern to follow):
```python
def load_analyst_registry(registry_path: Path) -> pd.DataFrame
def compile_and_write_market_anchors(industry_id: str) -> Path
```

From config/settings.py:
```python
DATA_RAW = Path("data/raw")
DATA_PROCESSED = Path("data/processed")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand private company YAML registry and implement private_valuations.py</name>
  <files>data/raw/private_companies/ai_private_registry.yaml, src/processing/private_valuations.py, tests/test_private_valuations.py</files>
  <read_first>
    - data/raw/private_companies/ai_private_registry.yaml (stub from Plan 10-01 — 3 entries)
    - src/processing/validate.py (PRIVATE_VALUATION_SCHEMA definition)
    - src/ingestion/market_anchors.py (compile_and_write_market_anchors pattern for Parquet write)
    - .planning/phases/10-revenue-attribution-and-private-company-valuation/10-RESEARCH.md (Pattern 1 YAML example, Pitfall 5 on stale multiples)
    - tests/test_private_valuations.py (existing test scaffold from Plan 10-01)
  </read_first>
  <behavior>
    - load_private_registry(path) loads YAML and returns DataFrame
    - apply_comparable_multiples(row_dict) computes implied_ev_low/mid/high from estimated_arr * low/mid/high multiples
    - compile_and_write_private_valuations("ai") loads YAML, validates with PRIVATE_VALUATION_SCHEMA, writes Parquet with provenance, returns Path
    - YAML registry has 15-20 private companies across ai_software, ai_infrastructure, ai_adoption segments
    - Every entry has confidence_tier, comparable_mid_multiple, vintage_date
    - implied_ev_low <= implied_ev_mid <= implied_ev_high for all entries
  </behavior>
  <action>
    1. Expand `data/raw/private_companies/ai_private_registry.yaml` from 3 stub entries to 18 entries. Each entry must include ALL fields from the research template: company_name, confidence_tier, last_funding_valuation_usd_billions, funding_date, estimated_arr_usd_billions, arr_source, arr_source_url (can be empty string if unavailable), comparable_low_multiple, comparable_mid_multiple, comparable_high_multiple, comparable_peer_group, implied_ev_low, implied_ev_mid, implied_ev_high, segment, vintage_date, notes.

       **HIGH confidence (recent funding round with known valuation):**
       - OpenAI: ARR 3.4B, multiples 25/40/60, EV 85/136/204B, segment ai_software, vintage 2024-10-01
       - Anthropic: ARR 0.9B, multiples 30/50/70, EV 27/45/63B, segment ai_software, vintage 2024-12-01
       - xAI: ARR 0.4B, multiples 30/50/75, EV 12/20/30B, segment ai_software, vintage 2024-12-01
       - Databricks: ARR 1.6B, multiples 15/25/35, EV 24/40/56B, segment ai_infrastructure, vintage 2024-02-01
       - CoreWeave: ARR 1.5B, multiples 10/15/22, EV 15/22.5/33B, segment ai_infrastructure, vintage 2024-11-01
       - Scale AI: ARR 0.75B, multiples 20/30/45, EV 15/22.5/33.75B, segment ai_software, vintage 2024-08-01

       **MEDIUM confidence (revenue estimate from press + comparable multiple):**
       - Mistral: ARR 0.15B, multiples 40/65/90, EV 6/9.75/13.5B, segment ai_software, vintage 2024-06-01
       - Cohere: ARR 0.08B, multiples 35/55/80, EV 2.8/4.4/6.4B, segment ai_software, vintage 2024-07-01
       - Hugging Face: ARR 0.07B, multiples 30/50/75, EV 2.1/3.5/5.25B, segment ai_software, vintage 2024-08-01
       - Together AI: ARR 0.1B, multiples 25/40/60, EV 2.5/4.0/6.0B, segment ai_infrastructure, vintage 2024-06-01
       - Anyscale: ARR 0.05B, multiples 20/35/50, EV 1.0/1.75/2.5B, segment ai_infrastructure, vintage 2024-03-01
       - Glean: ARR 0.1B, multiples 25/40/60, EV 2.5/4.0/6.0B, segment ai_software, vintage 2024-09-01
       - Perplexity: ARR 0.08B, multiples 40/65/100, EV 3.2/5.2/8.0B, segment ai_software, vintage 2024-12-01

       **LOW confidence (revenue unknown, valuation inferred from funding):**
       - Inflection AI: ARR unknown (0.02B estimate), multiples 50/80/120, EV 1.0/1.6/2.4B, segment ai_software, vintage 2024-03-01
       - Aleph Alpha: ARR unknown (0.01B estimate), multiples 30/50/80, EV 0.3/0.5/0.8B, segment ai_software, vintage 2024-01-01
       - AI21 Labs: ARR 0.03B, multiples 25/40/60, EV 0.75/1.2/1.8B, segment ai_software, vintage 2024-05-01
       - Runway: ARR 0.05B, multiples 30/50/75, EV 1.5/2.5/3.75B, segment ai_software, vintage 2024-06-01
       - Stability AI: ARR 0.05B, multiples 15/25/40, EV 0.75/1.25/2.0B, segment ai_software, vintage 2024-03-01

       Each entry must include a notes field explaining the comparables rationale and any caveats.

    2. Create `src/processing/private_valuations.py` with:
       - Module docstring explaining MODL-03 purpose (comparable multiples valuation for private AI companies)
       - `load_private_registry(registry_path: Path) -> pd.DataFrame` — same YAML loading pattern as load_analyst_registry
       - `apply_comparable_multiples(estimated_arr: float, low_mult: float, mid_mult: float, high_mult: float) -> tuple[float, float, float]` — returns (implied_ev_low, implied_ev_mid, implied_ev_high). This is a pure function for recomputation if ARR updates.
       - `compile_and_write_private_valuations(industry_id: str = "ai") -> Path`:
         1. Load YAML registry from DATA_RAW / "private_companies" / f"ai_private_registry.yaml"
         2. Construct DataFrame with all PRIVATE_VALUATION_SCHEMA columns
         3. Validate with PRIVATE_VALUATION_SCHEMA
         4. Assert implied_ev_low <= implied_ev_mid <= implied_ev_high for every row (raise ValueError if not)
         5. Write to DATA_PROCESSED / f"private_valuations_{industry_id}.parquet" using pyarrow with provenance metadata (source: "private_registry", industry, fetched_at)
         6. Return output Path
       - Imports: yaml, pandas, pyarrow, pathlib.Path, from config.settings import DATA_RAW, DATA_PROCESSED, from src.processing.validate import PRIVATE_VALUATION_SCHEMA

    3. Update `tests/test_private_valuations.py` — unskip placeholders and add:
       - test_compile_private_valuations: Call compile_and_write_private_valuations("ai"), assert Parquet exists, read back, verify >= 15 rows, verify all PRIVATE_VALUATION_SCHEMA columns present
       - test_ev_ordering_all_rows: Load compiled Parquet, assert implied_ev_low <= implied_ev_mid <= implied_ev_high for every row
       - test_confidence_tiers_present: Assert all three tiers (HIGH, MEDIUM, LOW) appear in the compiled output
       - test_no_missing_vintage_dates: Assert vintage_date is not null/empty for any row
       - test_multiples_reasonable: Assert comparable_mid_multiple is between 1.0 and 300.0 for all rows (matches schema check)
  </action>
  <verify>
    <automated>uv run pytest tests/test_private_valuations.py -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_private_valuations.py -v` shows all tests pass (0 failures)
    - `uv run python -c "import pandas as pd; df = pd.read_parquet('data/processed/private_valuations_ai.parquet'); print(f'{len(df)} companies'); assert len(df) >= 15; assert set(['HIGH','MEDIUM','LOW']).issubset(set(df['confidence_tier'])); print('OK')"` passes
    - `grep -c "entries" data/raw/private_companies/ai_private_registry.yaml` returns >= 1
    - `wc -l < data/raw/private_companies/ai_private_registry.yaml` returns >= 250
  </acceptance_criteria>
  <done>private_valuations_ai.parquet exists with 18 private companies; every company has confidence_tier, low/mid/high EV, vintage_date; implied_ev ordering holds for all rows</done>
</task>

<task type="auto">
  <name>Task 2: Wire private valuations into pipeline.py</name>
  <files>src/ingestion/pipeline.py, tests/test_pipeline_wiring.py</files>
  <read_first>
    - src/ingestion/pipeline.py (full file — check Step 8 from Plan 10-02 already exists)
    - src/processing/private_valuations.py (newly created — confirm compile_and_write_private_valuations exists)
    - tests/test_pipeline_wiring.py (check for existing attribution wiring test from Plan 10-02)
  </read_first>
  <action>
    1. Add Step 9 to `run_full_pipeline()` in pipeline.py, AFTER Step 8 (revenue attribution). Follow the same try/except pattern:

    ```python
    # Step 9: Private company valuations (compile YAML registry to Parquet)
    try:
        from src.processing.private_valuations import compile_and_write_private_valuations
        private_path = compile_and_write_private_valuations(industry_id)
        processed_paths["private_valuations"] = private_path
    except Exception as e:
        print(f"Private valuations failed: {e}")
    ```

    2. Add a wiring test to tests/test_pipeline_wiring.py:
       - test_private_valuations_step_in_pipeline: Import compile_and_write_private_valuations, call with "ai", assert returns Path ending in "private_valuations_ai.parquet"
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline_wiring.py -v --timeout=30 -k "private" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "compile_and_write_private_valuations" src/ingestion/pipeline.py` returns >= 1
    - `grep -c "Step 9" src/ingestion/pipeline.py` returns >= 1
    - `uv run pytest tests/test_pipeline_wiring.py -v -k "private"` shows test passes
  </acceptance_criteria>
  <done>pipeline.py Step 9 calls compile_and_write_private_valuations; private valuations run as part of run_full_pipeline; wiring test passes</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_private_valuations.py tests/test_pipeline_wiring.py -v --timeout=30` — all tests pass
- `uv run python -c "import pandas as pd; df = pd.read_parquet('data/processed/private_valuations_ai.parquet'); assert len(df) >= 15; assert (df['implied_ev_low'] <= df['implied_ev_mid']).all(); assert (df['implied_ev_mid'] <= df['implied_ev_high']).all(); print(f'MODL-03: {len(df)} private companies valued')"` — ordering invariant holds
</verification>

<success_criteria>
- private_valuations_ai.parquet exists with 15-20 company rows
- Every row has confidence_tier (HIGH/MEDIUM/LOW), comparable_mid_multiple, vintage_date
- implied_ev_low <= implied_ev_mid <= implied_ev_high for every row
- All three confidence tiers are represented
- Parquet has pyarrow provenance metadata
- Pipeline Step 9 is wired and callable
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-revenue-attribution-and-private-company-valuation/10-03-SUMMARY.md`
</output>
